# Функция "Входящие" (Inbox) — Задачи без привязки к целям

**Дата решения:** 2026-02-04
**Статус:** Утверждено, готово к реализации

---

## Проблема

Сейчас пользователь не может создать задачу без привязки к цели:
- Структура: Цель → Шаги
- QuickAddTask требует выбор цели (обязательное поле)
- Это неудобно для быстрых задач типа "Позвонить маме", "Купить молоко"

---

## Решение: Вариант 1 — "Входящие" как системная цель

Создать специальную системную цель "Входящие" (Inbox), куда попадают все задачи без привязки к конкретной цели.

### Преимущества
- Минимальные изменения архитектуры
- Сохраняется модель `step.goal_id` (не ломает бэкенд)
- Знакомый паттерн для пользователей Todoist
- Философия 1% сохраняется (задачи можно потом привязать к целям)

---

## Визуальная реализация

### 1. QuickAddTask — создание задачи

**До:**
```
🎯 Цель: [Выберите цель ▾]
         ⚠️ обязательно
```

**После:**
```
🎯 Цель: [Входящие ▾]
         (необязательно)
```

- По умолчанию выбрано "Входящие"
- Пользователь может оставить так или выбрать конкретную цель
- Ввёл текст → Enter → готово

### 2. Planning — отображение в календаре

```
📅 Понедельник, 5 февраля
─────────────────────────────────────

📥 ВХОДЯЩИЕ
┌─────────────────────────────────┐
│ ☐ Позвонить маме                │
│ ☐ Купить молоко                 │
│ ☐ Записаться к врачу            │
└─────────────────────────────────┘

💰 Увеличить доход на 20%
┌─────────────────────────────────┐
│ ☐ Изучить курс по инвестициям   │
│ ☐ Составить бюджет на месяц     │
└─────────────────────────────────┘
```

"Входящие" показывается как первая группа с особым стилем (иконка 📥).

### 3. Список целей (внизу Planning)

```
Цели и шаги
─────────────────────────────────────

📥 Входящие                    3 задачи
├── ☐ Позвонить маме
├── ☐ Купить молоко
└── ☐ Записаться к врачу    [→ В цель]
                             ↑
                    кнопка переместить
```

### 4. Перемещение задачи в цель

При клике на задачу из "Входящих" можно:
- Изменить дату, приоритет, время
- Переместить в конкретную цель (выбрать из списка)
- Оставить во "Входящих"

---

## Техническая реализация

### Бэкенд

1. Новое поле для модели Goal:
```python
is_inbox = BooleanField(default=False)
```

2. При регистрации пользователя автоматически создать цель "Входящие":
```python
Goal.objects.create(
    user=user,
    title="Входящие",
    is_inbox=True,
    # Эта цель не удаляется, не редактируется пользователем
)
```

3. API возвращает `inbox_goal_id` в данных пользователя.

### Фронтенд

1. **Store (app.js):**
```javascript
const inboxGoalId = ref(null)

// При загрузке данных пользователя
inboxGoalId.value = userData.inbox_goal_id
```

2. **QuickAddTask.vue:**
```javascript
// Цель становится опциональной
// По умолчанию = Inbox
const selectedGoal = ref(null) // null означает "Входящие"

// В save():
const goalId = selectedGoal.value?.id || store.inboxGoalId
```

3. **Planning.vue:**
- Показывать "Входящие" первой группой
- Особый стиль (иконка 📥, возможно другой цвет)
- Кнопка "Переместить в цель" для задач из Inbox

---

## UX Flow

```
Пользователь нажимает "+" (Quick Add)
        │
        ▼
Вводит "Позвонить маме"
        │
        ▼
Жмёт Enter (цель = Входящие по умолчанию)
        │
        ▼
Задача появляется в Planning на сегодня
(в группе "Входящие")
        │
        ▼
[Опционально] Позже переносит в цель "Улучшить отношения с семьёй"
```

---

## Альтернативы (отклонены)

### Вариант 2: Отдельная сущность Task
- Новая модель Task на бэкенде
- Больше работы, сложнее поддерживать
- Имеет смысл если будет отдельное приложение-планировщик

### Вариант 3: goal_id = nullable
- Разрешить steps без goal_id
- Ломает текущую архитектуру

---

## Контекст

Исследование основано на анализе Todoist:
- Inbox (Входящие) — все задачи по умолчанию попадают сюда
- Projects — опциональная организация
- Quick Add — минимальное трение

**Примечание:** В будущем планируется отдельное приложение-планировщик (как Todoist). Текущее решение — промежуточный шаг, который можно расширить.

---

## Файлы для изменения

### Фронтенд
- `src/components/QuickAddTask.vue` — убрать обязательность цели
- `src/stores/app.js` — добавить inboxGoalId
- `src/views/Planning.vue` — отображение группы "Входящие"

### Бэкенд (требуется)
- Модель Goal: добавить `is_inbox`
- Создание Inbox при регистрации
- API: возвращать `inbox_goal_id`
