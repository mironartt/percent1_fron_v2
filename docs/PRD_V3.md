# PRD: OnePercent V3 — Система достижения бизнес-целей

**Версия:** 3.0
**Дата:** 2026-02-15
**Статус:** Draft
**Основа:** [PRODUCT_CONCEPT_V3.md](PRODUCT_CONCEPT_V3.md)

---

## 1. Обзор продукта

### 1.1 Суть

OnePercent V3 — система, которая берёт бизнес-цель пользователя и доводит до результата: строит маршрут, исполняет шаги через AI и подключает подрядчиков. Для соло-предпринимателей, которые хотят расти, запускать новое и масштабироваться.

### 1.2 Формула

```
Пользователь говорит цель → Система строит маршрут → AI исполняет большую часть шагов
```

### 1.3 Целевая аудитория

| Сегмент | Боль | Точка входа |
|---|---|---|
| **Действующие предприниматели** | Хотят вырасти/масштабироваться, но вязнут в операционке | Экспресс-аудит бизнеса → Цель → Исполнение |
| **Начинающие с идеей** | Хотят запустить, но не знают с чего начать | Исследование рынка → Цель → Исполнение |
| **Начинающие без ниши** | Хотят свой бизнес, но не знают чем заняться | Лид-магнит «Подбор ниши» → Исследование → Цель |

### 1.4 Ключевые метрики успеха MVP

| Метрика | Целевое значение | Как измеряем |
|---|---|---|
| Конверсия регистрация → диагностика | > 60% | Analytics |
| Конверсия диагностика → создание цели | > 40% | Backend events |
| % шагов, выполненных через AI | > 50% от AI-шагов | Backend stats |
| Retention D7 | > 25% | Analytics |
| Среднее время до первого AI-результата | < 5 мин | Backend logs |

---

## 2. Архитектура трансформации

### 2.1 Текущее состояние (V2)

Продукт сейчас — система управления жизненными целями через 6 сфер (колесо баланса). Основные модули:

| Модуль | Файлы | Статус в V3 |
|---|---|---|
| SSP / Колесо баланса | `BalancedScorecard.vue`, store `app.js` (SSP-секция) | **Убираем из навигации** |
| Декомпозиция целей | `GoalEdit.vue` (202 KB), `GoalNew.vue` | **Трансформируем** — ядро V3 |
| Goals Bank | `GoalsBank.vue` (173 KB) | **Адаптируем** под бизнес-цели |
| AI Mentor | `MentorWidget.vue`, `MentorPanel.vue`, `MentorFloatingButton.vue` | **Трансформируем** — контекстный советник |
| Планировщик | `Planning.vue` (186 KB) | **Адаптируем** — статус исполнения |
| Журнал | `Journal.vue`, `JournalHistory.vue`, `JournalEntry.vue` | **Убираем из навигации** |
| Привычки | `Habits.vue` (221 KB), store `habits.js` | **Убираем из навигации** |
| XP / Геймификация | Store `xp.js`, `XPNotification.vue` | **Убираем из навигации** |
| Мини-задачи | `MiniTask.vue`, `MiniTaskWelcome.vue` | **Убираем из навигации** |
| Онбординг | `OnboardingAI.vue`, `OnboardingFast.vue` | **Полностью переделываем** |
| AI Proxy | `server/aiProxy.js` | **Расширяем** — новые промпты |
| Dashboard | `Dashboard.vue` (41 KB) | **Полностью переделываем** |

### 2.2 Целевое состояние (V3 MVP)

```
┌──────────────────────────────────────────────────────┐
│                    ONBOARDING V3                      │
│  «Есть бизнес?» → Трек 1 (Аудит) / Трек 2 (Идея)   │
└──────────────────────┬───────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────┐
│               ДИАГНОСТИКА (новый модуль)              │
│  Трек 1: URL сайта → AI-аудит (SEO, конкуренты...)   │
│  Трек 2: Описание идеи → AI-исследование рынка       │
└──────────────────────┬───────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────┐
│           ФОРМИРОВАНИЕ ЦЕЛИ (новый модуль)            │
│  AI предлагает цель на основе диагностики             │
│  Пользователь подтверждает / редактирует              │
└──────────────────────┬───────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────┐
│      ДЕКОМПОЗИЦИЯ + ИСПОЛНИТЕЛИ (модиф. GoalEdit)     │
│  Шаги с типом: AI делает / Сам / Подрядчик (скоро)   │
└──────────────────────┬───────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────┐
│            AI-ИСПОЛНЕНИЕ ШАГОВ (новый модуль)         │
│  Кнопка «AI, сделай» → результат внутри шага         │
└──────────────────────┬───────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────┐
│              DASHBOARD V3 (переделка)                 │
│  Что AI сделал / Что ждёт тебя / Прогресс к цели    │
└──────────────────────────────────────────────────────┘

Поддерживающие модули:
├── AI-ментор (контекстный, знает диагностику + прогресс)
└── Планировщик (статус исполнения: AI сделал / жду тебя)
```

---

## 3. Фазы разработки

### Фаза 0: Очистка (2-3 дня)

**Цель:** Убрать модули V2, которые не входят в MVP V3.

#### 3.0.1 Скрытие модулей из навигации

**Файлы:**
- [Sidebar.vue](../src/components/Sidebar.vue) — убрать пункты меню
- [BottomNavigation.vue](../src/components/BottomNavigation.vue) — убрать пункты
- [src/router/index.js](../src/router/index.js) — закомментировать маршруты (не удалять)

**Что скрываем:**
- `/app/ssp` — Колесо баланса
- `/app/journal` — Журнал
- `/app/habits` — Привычки
- `/app/learning` — Обучение
- Мини-задачи (убрать `miniTaskActive` логику из router guard)
- XP-виджеты с Dashboard

**Важно:** Код НЕ удаляем — только скрываем из UI и навигации. Маршруты оставляем закомментированными для возможного возврата в Фазе 2+.

#### 3.0.2 Очистка Dashboard

**Файл:** [Dashboard.vue](../src/views/Dashboard.vue)

Убрать виджеты:
- Колесо баланса (SSP-виджет)
- Привычки (HabitTracker)
- Журнал (JournalWidget)
- XP-бейджи
- Первые шаги (FirstSteps) — заменим на новый онбординг-прогресс

Оставить:
- Блок целей (адаптируем)
- AI-ментор (MentorPanel — адаптируем)

#### 3.0.3 Упрощение онбординга

**Файлы:**
- [OnboardingAI.vue](../src/components/OnboardingAI.vue) — деактивировать текущий флоу
- [OnboardingRouter.vue](../src/components/OnboardingRouter.vue) — переключить на новый

Текущий 5-шаговый онбординг (SSP → AI-анализ → генерация целей) заменяется на новый поток диагностики (Фаза 1).

**Критерий готовности:** Приложение запускается, навигация показывает только: Dashboard, Цели, Планировщик, Ментор. Все скрытые модули доступны по прямым URL (для дебага), но не видны в UI.

---

### Фаза 1: Диагностика — Точка А (5-7 дней)

**Цель:** Новый модуль, определяющий стартовую позицию пользователя.

#### 3.1.1 Новый стор: `src/stores/diagnosis.js`

```javascript
// Структура стейта
{
  // Профиль человека (собирается до выбора трека)
  profile: {
    weeklyHours: null,      // 5 | 10 | 20  (часов в неделю)
    budget: null,           // 'zero' | 'under_50k' | 'over_50k'
    // Дополнительные данные — дособирает AI-ментор позже:
    skills: [],             // ['лендинги', 'копирайтинг', ...]
    employment: null,       // 'fulltime' | 'parttime' | 'free'
    businessExperience: null, // 'none' | 'beginner' | 'experienced'
  },

  // Тип трека
  track: null,              // 'existing_business' | 'new_business'

  // Входные данные бизнеса
  input: {
    websiteUrl: '',         // Трек 1: URL сайта
    socialLinks: [],        // Трек 1: соцсети
    businessIdea: '',       // Трек 2: описание идеи
    niche: '',              // Трек 2: ниша
  },

  // Результаты диагностики
  results: {
    status: 'idle',         // 'idle' | 'loading' | 'completed' | 'error'
    sections: [],           // Массив секций аудита/исследования
    summary: '',            // Краткое резюме от AI
    suggestedGoals: [],     // Предложенные цели
    raw: null,              // Полный ответ AI
  },

  // Мета
  completedAt: null,
  backendId: null,
}
```

**Экшены стора:**

| Экшен | Описание |
|---|---|
| `setProfile(data)` | Сохраняет профиль человека (время, бюджет) |
| `updateProfileFromMentor(data)` | Дополняет профиль данными от AI-ментора (навыки, опыт) |
| `setTrack(type)` | Устанавливает трек (existing/new) |
| `setInput(data)` | Сохраняет входные данные бизнеса |
| `runDiagnosis()` | Запускает AI-диагностику через aiProxy |
| `saveDiagnosisToBackend()` | Синхронизирует с бэкендом |
| `loadDiagnosisFromBackend()` | Загружает сохранённую диагностику |
| `reset()` | Сбрасывает стор |

#### 3.1.2 Новый view: `src/views/Diagnosis.vue`

**Маршрут:** `/app/diagnosis`

**UI-флоу:**

```
Шаг 1: Профиль человека
┌─────────────────────────────────────┐
│                                     │
│  Пара вопросов, чтобы подобрать     │
│  реалистичный маршрут               │
│                                     │
│  Сколько часов в неделю готов       │
│  уделять бизнесу?                   │
│  [ 5ч ]  [ 10ч ]  [ 20ч+ ]         │
│                                     │
│  Какой бюджет на старт?             │
│  [ 0 руб ]  [ до 50К ]  [ 50К+ ]   │
│                                     │
│  [ Далее → ]                        │
└─────────────────────────────────────┘

Шаг 2: Выбор трека
┌─────────────────────────────────────┐
│                                     │
│  Что тебя сюда привело?             │
│                                     │
│  ┌─────────────┐ ┌───────────────┐  │
│  │ 🏢 У меня   │ │ 🚀 Запускаю   │  │
│  │ есть бизнес │ │ новый бизнес  │  │
│  └─────────────┘ └───────────────┘  │
│                                     │
└─────────────────────────────────────┘

Шаг 3a (Трек 1): Ввод данных бизнеса
┌─────────────────────────────────────┐
│  Вставь ссылку на сайт             │
│  [ https://...                   ]  │
│                                     │
│  Соцсети (необязательно)            │
│  [ + Добавить ссылку             ]  │
│                                     │
│  [ Запустить аудит →             ]  │
└─────────────────────────────────────┘

Шаг 3b (Трек 2): Ввод идеи
┌─────────────────────────────────────┐
│  Опиши свою идею или нишу           │
│  ┌─────────────────────────────┐    │
│  │ Textarea (5-7 строк)       │    │
│  │                             │    │
│  └─────────────────────────────┘    │
│                                     │
│  [ Исследовать рынок →          ]   │
└─────────────────────────────────────┘

Шаг 4: Результаты (общий для обоих треков)
┌─────────────────────────────────────┐
│  ✓ Диагностика завершена            │
│                                     │
│  ┌─ Секция 1: SEO-аудит ────────┐  │
│  │ Оценка: 3/10                  │  │
│  │ Детали: ...                   │  │
│  └───────────────────────────────┘  │
│  ┌─ Секция 2: Конкуренты ───────┐  │
│  │ ...                           │  │
│  └───────────────────────────────┘  │
│  ...                                │
│                                     │
│  Резюме: "Главная проблема —        │
│  отсутствие воронки..."             │
│                                     │
│  [ Сформировать цель →          ]   │
└─────────────────────────────────────┘
```

**Секции результатов:**

Трек 1 (Аудит бизнеса):
1. SEO-аудит
2. Аудит конкурентов
3. Аудит контента
4. Аудит воронки
5. Аудит репутации
6. Анализ позиционирования
7. SWOT-анализ

Трек 2 (Исследование рынка):
1. Жизнеспособность ниши
2. Конкуренты
3. Целевая аудитория
4. Анализ спроса
5. Требования для входа
6. MVP-рекомендация

**Каждая секция содержит:**
```javascript
{
  id: 'seo_audit',
  title: 'SEO-аудит',
  score: 3,            // 1-10 или null
  status: 'completed', // 'pending' | 'loading' | 'completed' | 'error'
  summary: '...',      // Краткий вывод
  details: '...',      // Полный текст (markdown)
  recommendations: [], // Рекомендации
}
```

#### 3.1.3 Расширение AI Proxy

**Файл:** [server/aiProxy.js](../server/aiProxy.js)

Новые эндпоинты:

| Эндпоинт | Метод | Описание |
|---|---|---|
| `/api/ai/diagnosis/audit` | POST | Запуск аудита бизнеса (Трек 1) |
| `/api/ai/diagnosis/research` | POST | Запуск исследования рынка (Трек 2) |
| `/api/ai/diagnosis/goal` | POST | Генерация цели из диагностики |

**Промпты:**

Каждый промпт для диагностики должен быть отдельным файлом или секцией в aiProxy. Промпты получают:
- Профиль человека (доступное время, бюджет, навыки если есть)
- Входные данные бизнеса (URL / описание идеи)
- Результаты предыдущих секций (для SWOT — все предыдущие)

**Формат запроса (аудит):**
```json
{
  "profile": {
    "weeklyHours": 10,
    "budget": "under_50k"
  },
  "track": "existing_business",
  "websiteUrl": "https://example.com",
  "socialLinks": ["https://instagram.com/example"],
  "sections": ["seo", "competitors", "content", "funnel", "reputation", "positioning", "swot"]
}
```

**Формат запроса (исследование):**
```json
{
  "profile": {
    "weeklyHours": 20,
    "budget": "zero"
  },
  "track": "new_business",
  "businessIdea": "SaaS для автоматизации рассылок",
  "niche": "email-маркетинг для инфобизнеса"
}
```

**Формат ответа:**
```json
{
  "sections": [
    {
      "id": "seo_audit",
      "title": "SEO-аудит",
      "score": 3,
      "summary": "Сайт практически невидим в поиске...",
      "details": "### Текущее состояние\n...",
      "recommendations": [
        "Добавить мета-теги на все страницы",
        "Создать блог с ключевыми запросами"
      ]
    }
  ],
  "summary": "Главная проблема — отсутствие воронки продаж...",
  "suggestedGoals": [
    {
      "title": "Запустить онлайн-курс как лид-магнит",
      "rationale": "На основе аудита: у вас нет лид-магнита..."
    }
  ]
}
```

#### 3.1.4 Backend API (Django)

Необходимые эндпоинты на бэкенде:

| Эндпоинт | Метод | Описание |
|---|---|---|
| `/api/rest/front/diagnosis/` | POST | Сохранить результаты диагностики |
| `/api/rest/front/diagnosis/` | GET | Получить сохранённую диагностику |
| `/api/rest/front/diagnosis/{id}/` | PATCH | Обновить диагностику |

**Модель данных (Django):**
```python
class Diagnosis(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    track = models.CharField(max_length=20)  # 'existing_business' / 'new_business'
    input_data = models.JSONField()           # URL, соцсети или описание идеи
    results = models.JSONField()              # Полные результаты диагностики
    summary = models.TextField(blank=True)
    suggested_goals = models.JSONField(default=list)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

**Критерий готовности:** Пользователь выбирает трек → вводит данные → получает полный отчёт диагностики → результаты сохранены в бэкенде.

---

### Фаза 2: Цель из диагностики (2 дня)

**Цель:** AI генерирует бизнес-цель на основе результатов диагностики.

#### 3.2.1 Новый view: `src/views/GoalFromDiagnosis.vue`

**Маршрут:** `/app/diagnosis/goal`

**UI-флоу:**

```
┌─────────────────────────────────────┐
│  На основе диагностики мы           │
│  рекомендуем цель:                  │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ «Запустить онлайн-курс как  │    │
│  │  лид-магнит за 4 недели»    │    │
│  │                             │    │
│  │  Почему: У вас 0 органичес- │    │
│  │  кого трафика и нет лид-    │    │
│  │  магнита. Курс привлечёт    │    │
│  │  целевую аудиторию...       │    │
│  └─────────────────────────────┘    │
│                                     │
│  [ ✓ Принять ]  [ ✏ Изменить ]      │
│  [ Другая цель ]                    │
│                                     │
└─────────────────────────────────────┘
```

**Логика:**
1. Загружаем результаты диагностики из `diagnosis` стора
2. Показываем предложенную цель (из `suggestedGoals[0]`)
3. Пользователь может:
   - **Принять** → создаём цель через `createGoal()` из `api.js` → переход к декомпозиции
   - **Изменить** → inline-редактирование заголовка и описания → создание
   - **Другая цель** → показать остальные `suggestedGoals` или ввести свою

**Интеграция с существующим стором:**
- Создание цели через существующий `addGoal()` в `stores/app.js`
- Добавить поле `diagnosisId` к цели для привязки к диагностике
- Категория цели: новая категория `business` или маппинг на `career` → `work`

**Критерий готовности:** Пользователь видит предложенную цель → подтверждает/редактирует → цель создана в системе и привязана к диагностике.

---

### Фаза 3: Декомпозиция + типы исполнителей (3-4 дня)

**Цель:** Каждый шаг цели получает тип исполнителя: AI / Сам / Подрядчик (неактивно).

#### 3.3.1 Модификация модели шага

**Файл:** [stores/app.js](../src/stores/app.js) — секция работы со steps

Добавить к модели шага:

```javascript
{
  // Существующие поля (без изменений)
  id: 'uuid',
  title: '...',
  completed: false,
  priority: 'important',
  time_estimate: '1h',
  scheduled_date: null,
  comment: '',
  order: 0,

  // НОВЫЕ поля V3
  executor_type: 'self',       // 'ai' | 'self' | 'contractor'
  ai_status: null,             // null | 'pending' | 'running' | 'completed' | 'error'
  ai_result: null,             // Результат выполнения AI (markdown)
  ai_result_updated_at: null,  // Когда AI выполнил
  contractor_status: 'locked', // 'locked' (V3 MVP — всегда locked)
}
```

#### 3.3.2 Модификация GoalEdit.vue

**Файл:** [GoalEdit.vue](../src/views/GoalEdit.vue)

**Изменения в UI шага:**

```
Текущий вид шага:
┌──────────────────────────────────────────┐
│ ☐ Исследовать конкурентов               │
│    Приоритет: Важный  |  Время: 2ч      │
└──────────────────────────────────────────┘

Новый вид шага:
┌──────────────────────────────────────────┐
│ ☐ Исследовать конкурентов               │
│    🤖 AI делает  |  Приоритет: Важный    │
│    ┌──────────────────────────────┐      │
│    │  [ ▶ AI, сделай ]            │      │  ← если ai_status === null
│    │  [ ⏳ AI выполняет... ]       │      │  ← если ai_status === 'running'
│    │  [ ✓ Результат готов ▼ ]     │      │  ← если ai_status === 'completed'
│    └──────────────────────────────┘      │
└──────────────────────────────────────────┘
```

**Визуальное различие типов:**

| Тип | Иконка (Lucide) | Цвет бейджа | Интерактив |
|---|---|---|---|
| `ai` | `Bot` | Фиолетовый (`#8B5CF6`) | Кнопка «AI, сделай» |
| `self` | `User` | Синий (`#3B82F6`) | Стандартный чекбокс |
| `contractor` | `Users` | Серый (`#9CA3AF`) | Неактивно, тултип «Скоро» |

**Selector типа исполнителя:**

При создании/редактировании шага — dropdown или сегментированный контрол:

```
Исполнитель: [ 🤖 AI ]  [ 👤 Сам ]  [ 👥 Подрядчик (скоро) ]
```

#### 3.3.3 AI-генерация декомпозиции с типами

При создании цели из диагностики, AI генерирует шаги с уже назначенными типами:

**Промпт для aiProxy:**

Вход:
```json
{
  "goal": { "title": "...", "description": "..." },
  "diagnosis": { "track": "...", "results": {...} },
  "userContext": { "skills": [], "budget": null, "time": null }
}
```

Выход:
```json
{
  "steps": [
    {
      "title": "Исследовать конкурентов",
      "executor_type": "ai",
      "rationale": "AI может автоматически проанализировать...",
      "priority": "critical",
      "time_estimate": "30min"
    },
    {
      "title": "Записать 5 видео-уроков",
      "executor_type": "self",
      "rationale": "Требует личного участия — запись на камеру",
      "priority": "important",
      "time_estimate": "4h"
    }
  ]
}
```

#### 3.3.4 Backend API изменения

Расширить модель Step на бэкенде:

```python
class Step(models.Model):
    # Существующие поля...

    # Новые поля V3
    executor_type = models.CharField(
        max_length=20,
        choices=[('ai', 'AI'), ('self', 'Self'), ('contractor', 'Contractor')],
        default='self'
    )
    ai_status = models.CharField(
        max_length=20, null=True, blank=True,
        choices=[('pending', 'Pending'), ('running', 'Running'),
                 ('completed', 'Completed'), ('error', 'Error')]
    )
    ai_result = models.TextField(null=True, blank=True)
    ai_result_updated_at = models.DateTimeField(null=True, blank=True)
```

Обновить сериализатор для включения новых полей в API.

**Критерий готовности:** Шаги отображаются с типом исполнителя. AI-шаги визуально отличаются. Тип можно менять. При AI-генерации шагов типы назначаются автоматически.

---

### Фаза 4: AI-исполнение шагов (4-5 дней)

**Цель:** Кнопка «AI, сделай» запускает выполнение шага и показывает результат.

#### 3.4.1 Механика исполнения

**Пользовательский флоу:**
1. Пользователь видит шаг с типом `ai`
2. Нажимает «AI, сделай»
3. UI показывает прогресс (анимация, стриминг текста)
4. Результат появляется внутри шага (collapsible markdown)
5. Пользователь может: принять (отметить выполненным), перегенерировать, отредактировать

**Стейт-машина AI-шага:**

```
null → [нажал кнопку] → pending → [AI начал] → running → [AI закончил] → completed
                                                    ↘ [ошибка] → error
completed → [перегенерация] → pending → ...
```

#### 3.4.2 Новый эндпоинт aiProxy

**Файл:** [server/aiProxy.js](../server/aiProxy.js)

| Эндпоинт | Метод | Описание |
|---|---|---|
| `/api/ai/step/execute` | POST | Запустить AI-исполнение шага |
| `/api/ai/step/execute/stream` | POST (SSE) | То же, со стримингом ответа |

**Запрос:**
```json
{
  "step": {
    "id": "...",
    "title": "Исследовать конкурентов",
    "comment": "Дополнительный контекст..."
  },
  "goal": {
    "title": "Запустить онлайн-курс",
    "description": "..."
  },
  "diagnosis": {
    "summary": "...",
    "relevantSections": [...]
  },
  "previousSteps": [
    { "title": "...", "ai_result": "..." }
  ]
}
```

**Ответ:**
```json
{
  "result": "## Анализ конкурентов\n\n### 1. Прямые конкуренты\n...",
  "status": "completed",
  "tokensUsed": 1500
}
```

**Контекст промпта:** AI получает полный контекст — диагностику, цель, все предыдущие шаги и их результаты. Это обеспечивает связность: шаг 6 «Написать письма для рассылки» учитывает данные из шага 1 «Исследование конкурентов».

#### 3.4.3 UI компонент результата

**Новый компонент:** `src/components/AIStepResult.vue`

```
┌─ Результат AI ──────────────────────────┐
│                                          │
│  ## Анализ конкурентов                   │
│                                          │
│  ### 1. Прямые конкуренты                │
│  | Конкурент | Цена | Аудитория |        │
│  |---|---|---|                            │
│  | X | ... | ... |                       │
│  ...                                     │
│                                          │
│  ────────────────────────────────────    │
│  [ ✓ Принять ]  [ 🔄 Перегенерировать ] │
│  [ ✏ Редактировать ]  [ 📋 Копировать ] │
└──────────────────────────────────────────┘
```

**Функциональность:**
- Рендеринг markdown → HTML (использовать `marked` или аналог)
- Collapsible: по умолчанию свёрнут с превью (3 строки)
- Кнопка «Копировать» — в буфер обмена
- Кнопка «Редактировать» — inline-редактирование markdown
- Кнопка «Перегенерировать» — повторный запрос с опциональным уточнением
- Кнопка «Принять» — отмечает шаг выполненным

#### 3.4.4 Стриминг (опционально для MVP)

Если используем SSE (Server-Sent Events):
- `aiProxy.js` стримит ответ OpenAI chunk по chunk
- Фронт показывает текст по мере генерации (как в ChatGPT)
- Используем существующую инфраструктуру из [stores/aiTasks.js](../src/stores/aiTasks.js) — WebSocket уже настроен

Если не используем стриминг:
- Показываем skeleton/spinner
- Примерное время ожидания: 10-30 секунд

**Рекомендация:** Реализовать стриминг — UX значительно лучше, и инфраструктура уже есть.

#### 3.4.5 Backend API

| Эндпоинт | Метод | Описание |
|---|---|---|
| `/api/rest/front/steps/{id}/ai-execute/` | POST | Запустить AI-исполнение |
| `/api/rest/front/steps/{id}/ai-result/` | GET | Получить результат |
| `/api/rest/front/steps/{id}/ai-result/` | PATCH | Обновить результат (редактирование) |

**Критерий готовности:** Пользователь нажимает «AI, сделай» → видит прогресс → получает развёрнутый результат → может принять, перегенерировать или отредактировать.

---

### Фаза 5: Dashboard V3 (3-4 дня)

**Цель:** Новый главный экран, фокус на прогрессе к цели.

#### 3.5.1 Структура нового Dashboard

**Файл:** [Dashboard.vue](../src/views/Dashboard.vue) — полная переделка

```
┌──────────────────────────────────────────────────────┐
│  Привет, Маша 👋                                     │
│                                                      │
│  ┌─ Твоя цель ──────────────────────────────────┐   │
│  │  Запустить онлайн-курс как лид-магнит         │   │
│  │  ████████████░░░░░░░░  57% (4/7 шагов)       │   │
│  │  [ Перейти к цели → ]                         │   │
│  └───────────────────────────────────────────────┘   │
│                                                      │
│  ┌─ AI уже сделал ──────────────────────────────┐   │
│  │  ✓ Исследование конкурентов         готово    │   │
│  │  ✓ Письма для рассылки (6 шт.)     готово    │   │
│  │  ⏳ Контент-план                    в работе  │   │
│  └───────────────────────────────────────────────┘   │
│                                                      │
│  ┌─ Ждёт тебя ─────────────────────────────────┐   │
│  │  ○ Написать программу курса    ⏱ ~2ч        │   │
│  │  ○ Записать 5 видео-уроков     ⏱ ~4ч        │   │
│  └───────────────────────────────────────────────┘   │
│                                                      │
│  ┌─ AI-ментор ──────────────────────────────────┐   │
│  │  💬 "Маша, конкуренты проанализированы.       │   │
│  │  Рекомендую начать с программы курса —        │   │
│  │  это разблокирует 3 следующих шага."          │   │
│  │  [ Спросить совет ]                           │   │
│  └───────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────┘
```

#### 3.5.2 Секции Dashboard

| Секция | Данные | Логика |
|---|---|---|
| **Прогресс цели** | Заголовок, % выполнения, кол-во шагов | Основная цель (первая незавершённая) |
| **AI уже сделал** | Шаги с `executor_type: 'ai'` и `ai_status: 'completed'` | Последние 3-5, ссылка на полный список |
| **Ждёт тебя** | Шаги с `executor_type: 'self'` и `completed: false` | Ближайшие по приоритету/дате |
| **AI-ментор** | Контекстная рекомендация | На основе текущего прогресса |

#### 3.5.3 Пустые состояния

| Состояние | Что показываем |
|---|---|
| Нет диагностики | CTA «Начать диагностику» → `/app/diagnosis` |
| Диагностика есть, нет цели | CTA «Сформировать цель» → `/app/diagnosis/goal` |
| Цель есть, нет шагов | CTA «Декомпозировать цель» → `/app/goals/:id` |
| Все шаги выполнены | Поздравление + CTA «Поставить новую цель» |

**Критерий готовности:** Dashboard показывает реальный прогресс, AI-результаты, задачи пользователя и контекстного ментора. Все пустые состояния обработаны.

---

### Фаза 6: Тестирование и доработка (2-3 дня)

#### 3.6.1 E2E тестирование

| Сценарий | Шаги | Ожидаемый результат |
|---|---|---|
| Полный путь Трек 1 | Регистрация → Выбор «Есть бизнес» → Ввод URL → Диагностика → Цель → Декомпозиция → AI-исполнение | Все шаги проходят без ошибок |
| Полный путь Трек 2 | Регистрация → Выбор «Новый бизнес» → Ввод идеи → Диагностика → Цель → Декомпозиция → AI-исполнение | Все шаги проходят без ошибок |
| Пустые состояния | Новый пользователь → Dashboard | Корректные CTA |
| Перегенерация AI | Выполнить шаг → Перегенерировать → Проверить новый результат | Результат обновился |
| Редактирование цели | Изменить предложенную цель → Декомпозиция | Шаги соответствуют изменённой цели |
| Мобильная версия | Все экраны на 375px | Адаптивная вёрстка |

#### 3.6.2 Чек-лист качества

- [ ] Все API-ошибки обрабатываются (toast notification)
- [ ] Loading-состояния на всех async-операциях
- [ ] CSRF-токен передаётся корректно
- [ ] Rate limiting не блокирует UX
- [ ] localStorage синхронизация работает
- [ ] Навигация: скрытые модули недоступны из UI
- [ ] AI-промпты дают качественные результаты (ручная проверка)
- [ ] Стриминг работает без обрывов
- [ ] Мобильная адаптация всех новых экранов

---

## 4. Обновление навигации и маршрутов

### 4.1 Новая структура навигации

**Sidebar / BottomNavigation:**

```
📊 Dashboard          → /app
🔍 Диагностика        → /app/diagnosis
🎯 Цели               → /app/goals-bank
📅 Планировщик         → /app/planning
💬 Ментор              → /app/mentor (или floating)
```

### 4.2 Новые маршруты

**Файл:** [src/router/index.js](../src/router/index.js)

```javascript
// Новые маршруты V3
{
  path: '/app/diagnosis',
  name: 'Diagnosis',
  component: () => import('@/views/Diagnosis.vue'),
  meta: { requiresAuth: true, title: 'Диагностика' }
},
{
  path: '/app/diagnosis/goal',
  name: 'GoalFromDiagnosis',
  component: () => import('@/views/GoalFromDiagnosis.vue'),
  meta: { requiresAuth: true, title: 'Формирование цели' }
},
```

### 4.3 Новый Guard: Redirect на диагностику

Вместо текущего onboarding redirect:

```javascript
// Было: finish_onboarding === false → /onboarding
// Стало: diagnosis_completed === false → /app/diagnosis
```

---

## 5. Новые файлы (создать)

| Файл | Тип | Описание |
|---|---|---|
| `src/stores/diagnosis.js` | Store | Стор диагностики |
| `src/views/Diagnosis.vue` | View | Страница диагностики |
| `src/views/GoalFromDiagnosis.vue` | View | Формирование цели из диагностики |
| `src/components/AIStepResult.vue` | Component | Отображение AI-результата в шаге |
| `src/components/ExecutorBadge.vue` | Component | Бейдж типа исполнителя |
| `src/components/DiagnosisSection.vue` | Component | Секция результатов диагностики |

## 6. Модифицируемые файлы

| Файл | Что меняем |
|---|---|
| `src/stores/app.js` | Модель шага (новые поля), убрать SSP/miniTask из guard-логики |
| `src/views/GoalEdit.vue` | UI шагов: тип исполнителя, AI-кнопка, результат |
| `src/views/Dashboard.vue` | Полная переделка под V3 |
| `src/components/Sidebar.vue` | Новые пункты меню, убрать старые |
| `src/components/BottomNavigation.vue` | Аналогично |
| `src/router/index.js` | Новые маршруты, обновлённые guards |
| `src/services/api.js` | Новые API-функции для диагностики и AI-исполнения |
| `server/aiProxy.js` | Новые эндпоинты и промпты |

---

## 7. Зависимости от бэкенда (Django)

**Критичные для MVP:**

| Что нужно | Приоритет | Блокирует |
|---|---|---|
| Модель `Diagnosis` + API CRUD | P0 | Фазу 1 |
| Новые поля в модели `Step` (`executor_type`, `ai_status`, `ai_result`) | P0 | Фазу 3 |
| Эндпоинт `/steps/{id}/ai-execute/` | P0 | Фазу 4 |
| Замена `finish_onboarding` на `diagnosis_completed` (или добавление нового флага) | P1 | Фазу 5 (guards) |
| Обновление сериализаторов для новых полей | P0 | Фазы 3-4 |

**Можно отложить:**
- Токены/биллинг — Фаза 2+
- Подрядчики API — Фаза 2+
- Лид-магниты — Фаза 2+

---

## 8. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|---|---|---|---|
| Низкое качество AI-диагностики | Высокая | Высокое | Итеративная доработка промптов. Ручная проверка 10+ кейсов перед релизом |
| Долгое время генерации (>30 сек) | Средняя | Среднее | Стриминг SSE. Кэширование промежуточных результатов |
| Бэкенд не успевает | Средняя | Высокое | Начать с фронт-only прототипа (localStorage). Бэкенд-интеграция как отдельный шаг |
| Пользователи не понимают ценность диагностики | Средняя | Высокое | A/B тест: с диагностикой vs без. Онбординг-тултипы |
| OpenAI rate limits | Низкая | Высокое | Очередь запросов. Fallback на другую модель |

---

## 9. Вне скоупа MVP (Фаза 2+)

Следующие фичи **не входят** в MVP, но запланированы:

1. **Подрядчики** — кнопка «Заказать» → генерация ТЗ → интеграция Kwork/YouDo
2. **Лид-магниты** — отдельные лендинги «Подбор ниши» и «Экспресс-аудит»
3. **Токены** — монетизация AI-исполнения, подсчёт расхода
4. **Маркетинговый автопилот** — генерация лендингов, контента, рассылок
5. **Возврат модулей** — журнал, привычки, геймификация как поддерживающие
6. **Мультицелевость** — работа с несколькими целями параллельно

---

## 10. Таймлайн

| Фаза | Что | Дни | Зависимости |
|---|---|---|---|
| **0** | Очистка UI | 2-3 | — |
| **1** | Диагностика | 5-7 | Backend: модель Diagnosis |
| **2** | Цель из диагностики | 2 | Фаза 1 |
| **3** | Декомпозиция + исполнители | 3-4 | Backend: поля Step |
| **4** | AI-исполнение | 4-5 | Фазы 1-3, Backend: ai-execute endpoint |
| **5** | Dashboard V3 | 3-4 | Фазы 1-4 |
| **6** | Тестирование | 2-3 | Все фазы |
| | **Итого** | **~3 недели** | |

---

## Приложение A: Глоссарий

| Термин | Описание |
|---|---|
| **Диагностика** | Анализ текущего состояния бизнеса/идеи пользователя |
| **Трек** | Тип пользовательского пути (существующий бизнес / новый) |
| **Декомпозиция** | Разбиение цели на конкретные шаги |
| **Исполнитель** | Кто выполняет шаг: AI, пользователь или подрядчик |
| **AI-исполнение** | Автоматическое выполнение шага через AI (ресерч, текст, план) |
| **Точка А** | Стартовая позиция пользователя, определённая диагностикой |
