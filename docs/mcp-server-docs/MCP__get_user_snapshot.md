# get_user_snapshot

Инструмент для получения агрегированных данных о пользователе из системы личного развития OnePercent.

## Назначение

Позволяет AI получить полный контекст о целях, привычках, прогрессе и других аспектах жизни пользователя.

**Важно:**
- Read-only — НЕ изменяет никакие данные
- Можно вызывать БЕЗ подтверждения пользователя
- Безопасен для частого использования

## Когда использовать

### 1. Ответы на вопросы о прогрессе
- "Какие у меня цели?" → `include: ["goals"]`
- "Как идут мои привычки?" → `include: ["habits"]`
- "Сколько у меня XP?" → `include: ["xp"]`
- "Какой у меня баланс жизни?" → `include: ["ssp"]`

### 2. Перед созданием/изменением данных
- Перед созданием цели — проверить существующие цели
- Перед созданием привычки — посмотреть текущие привычки
- Перед планированием — получить расписание и цели

### 3. Для персонализированных рекомендаций
- Анализ баланса жизненных сфер (ssp)
- Анализ серий выполнения (habits.current_streak)
- Рекомендации на основе прогресса целей

### 4. Для контекстных диалогов
- Утреннее планирование → `include: ["schedule", "habits"]`
- Вечерний обзор → `include: ["diary", "habits", "xp"]`
- Еженедельный обзор → `include: ["goals", "habits", "ssp"]`

---

## Параметры

### include (array)

Какие секции данных включить в ответ.

| Секция | Описание |
|--------|----------|
| `goals` | Цели пользователя с категориями, статусами и оценками |
| `habits` | Привычки с статистикой выполнения и серий |
| `diary` | Записи дневника за период |
| `schedule` | Запланированные шаги целей на период |
| `xp` | Баланс опыта, уровень и прогресс |
| `ssp` | Система Сфер Приоритетов — оценка 6 жизненных сфер |
| `subscription` | Информация о тарифе и подписке |

**По умолчанию:** `["goals", "habits", "xp"]`

**Рекомендации по выбору:**

| Сценарий | Рекомендуемые секции |
|----------|---------------------|
| Общий контекст диалога | `["goals", "habits", "xp"]` |
| Утреннее планирование | `["schedule", "habits"]` |
| Вечерний обзор дня | `["diary", "habits", "xp"]` |
| Еженедельный анализ | `["goals", "habits", "ssp"]` |
| Анализ баланса жизни | `["ssp", "goals"]` |
| Полный контекст | все 7 секций |

### date_from (string)

Начало периода для фильтрации данных секций `schedule` и `diary`.

- **Формат:** YYYY-MM-DD (ISO 8601)
- **По умолчанию:** сегодняшняя дата
- **Влияет на:** schedule, diary
- **НЕ влияет на:** goals, habits, xp, ssp, subscription

### date_to (string)

Конец периода для фильтрации данных.

- **Формат:** YYYY-MM-DD (ISO 8601)
- **По умолчанию:** date_from + 7 дней
- **Ограничение:** рекомендуемый максимум 30 дней

### goals_status (string)

Фильтр целей по статусу выполнения.

| Значение | Описание |
|----------|----------|
| `work` | Активные цели (по умолчанию) |
| `complete` | Завершённые цели |
| `all` | Все цели |

---

## Структура ответа

```json
{
  "success": true,
  "user_name": "Иван",
  "data": {
    "goals": {...},
    "habits": {...},
    "diary": {...},
    "schedule": {...},
    "xp": {...},
    "ssp": {...},
    "subscription": {...}
  }
}
```

---

## Детальное описание секций

### Секция: goals

Цели пользователя — основа системы личного развития.

```json
{
  "items": [
    {
      "id": 123,
      "title": "Выучить Python",
      "category": "work",
      "status": "work",
      "score": "true"
    }
  ],
  "total_count": 5
}
```

**Поле category (жизненная сфера):**

| Значение | Описание |
|----------|----------|
| `welfare` | Благосостояние — финансы, доход |
| `hobby` | Хобби и отдых — увлечения, развлечения |
| `environment` | Дружба и окружение — друзья, социальные связи |
| `health_sport` | Здоровье и спорт — физическое здоровье, фитнес |
| `work` | Работа и карьера — профессиональное развитие |
| `family` | Любовь, семья, отношения — личная жизнь |

**Поле status:**

| Значение | Описание |
|----------|----------|
| `work` | В работе — активная цель |
| `complete` | Завершена — цель достигнута |
| `null` | Не оценена — новая цель |

**Поле score (истинная/ложная цель):**

Методология OnePercent: истинные цели — те, что действительно важны человеку.

| Значение | Описание |
|----------|----------|
| `"true"` | Истинная цель — соответствует глубинным ценностям |
| `"false"` | Ложная цель — навязана извне |
| `null` | Не оценена |

---

### Секция: habits

Привычки — ежедневные действия с системой XP для мотивации.

```json
{
  "items": [
    {
      "id": 45,
      "name": "Утренняя зарядка",
      "icon": "🏃",
      "frequency": "daily",
      "xp_reward": 15,
      "current_streak": 7,
      "best_streak": 14
    }
  ],
  "total_count": 5,
  "today_completed": 3,
  "today_total": 5,
  "weekly_completion_rate": 85
}
```

**Поле frequency:**

| Значение | Описание |
|----------|----------|
| `daily` | Ежедневно (Пн-Вс) |
| `weekdays` | Будни (Пн-Пт) |
| `weekends` | Выходные (Сб-Вс) |
| `custom` | Свой график |

**Статистика:**
- `current_streak` — текущая серия дней БЕЗ пропусков
- `best_streak` — лучшая серия за всё время
- `today_completed` / `today_total` — прогресс сегодня
- `weekly_completion_rate` — процент выполнения за неделю (0-100)

---

### Секция: diary

Дневник — записи для ежедневной рефлексии.

```json
{
  "items": [
    {
      "id": 789,
      "date": "2026-01-27T...",
      "what_done": "Завершил 3 шага к цели Python...",
      "what_not_done": "Не успел сделать домашку",
      "reflection": "День был продуктивным",
      "plans": "Завтра закончить Django ORM"
    }
  ],
  "total_count": 15
}
```

**Поля записи дневника:**

| Поле | Описание |
|------|----------|
| `what_done` | Что сделано сегодня |
| `what_not_done` | Что не удалось сделать |
| `reflection` | Рефлексия, выводы дня |
| `plans` | Планы на следующий день |

---

### Секция: schedule

Запланированные шаги целей на период.

```json
{
  "today": [
    {
      "id": 456,
      "title": "Прочитать главу 3",
      "goal_title": "Выучить Python",
      "date": "2026-01-27",
      "is_complete": false,
      "priority": "important"
    }
  ],
  "upcoming": [...]
}
```

**Поле priority:**

| Значение | Описание |
|----------|----------|
| `critical` | Критично — требует немедленного внимания |
| `important` | Важно — значимая задача |
| `attention` | Внимание — требует внимания |
| `optional` | Опционально — можно отложить |
| `null` | Без приоритета |

---

### Секция: xp

XP (Experience Points) — система опыта для геймификации.

```json
{
  "balance": 1500,
  "lifetime": 12500,
  "level": 2,
  "level_progress": 45,
  "next_level_xp": 2000
}
```

**Источники XP:**

| Источник | XP |
|----------|-----|
| habit_completed | +5 до +50 |
| habit_penalty | -5 до -50 |
| goal_step_completed | +25 |
| goal_completed | +150 |
| journal_entry | +10 |
| achievement_bonus | +50 до +500 |

---

### Секция: ssp

ССП — Система Сфер Приоритетов (Колесо Баланса).

```json
{
  "spheres": [
    {
      "category": "welfare",
      "name": "Благосостояние",
      "score": 7
    }
  ],
  "average_score": 6.5,
  "ssp_evaluation_id": "uuid..."
}
```

**Интерпретация score (1-10):**

| Диапазон | Интерпретация |
|----------|---------------|
| 1-3 | Критический дисбаланс — требует срочного внимания |
| 4-5 | Ниже среднего — есть проблемы |
| 6-7 | Нормально — средний уровень |
| 8-9 | Хорошо — сфера развита |
| 10 | Идеально — полная удовлетворённость |

---

### Секция: subscription

Информация о тарифе и подписке.

```json
{
  "tariff": "pro",
  "status": "active",
  "days_remaining": 25
}
```

**Поле tariff:**

| Значение | Описание |
|----------|----------|
| `free` | Бесплатный — базовый функционал |
| `pro` | Pro — полный функционал с AI |
| `club` | Клуб 1% — premium |

**Поле status:**

| Значение | Описание |
|----------|----------|
| `trial` | Триальный период |
| `active` | Активна |
| `expired` | Истекла |
| `cancelled` | Отменена |

---

## Примеры

### Пример 1: Получить цели и XP

```json
{
  "include": ["goals", "xp"],
  "goals_status": "work"
}
```

### Пример 2: Утреннее планирование

```json
{
  "include": ["schedule", "habits"],
  "date_from": "2026-01-27",
  "date_to": "2026-01-27"
}
```

### Пример 3: Полный анализ

```json
{
  "include": ["goals", "habits", "diary", "schedule", "xp", "ssp", "subscription"],
  "date_from": "2026-01-20",
  "date_to": "2026-01-27"
}
```

---

## Рекомендации AI

1. **Начало диалога:** вызови `get_user_snapshot(include=["goals", "habits", "xp"])` для контекста
2. **Мотивация:** используй `current_streak` для похвалы: "Отличная серия — 12 дней!"
3. **Рекомендации:** сравни `ssp.spheres` с `goals.category` — цели должны быть в "проблемных" сферах
4. **Планирование:** используй `schedule.today` для обзора дня
5. **Геймификация:** упоминай XP: "За эту задачу +25 XP!"
