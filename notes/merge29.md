# Merge 29: Объединение страниц Year Review (Results + Plan)

**Дата:** 14 декабря 2025  
**Задача:** Объединить страницы `/land/newyear/results` и `/land/newyear/plan` в одну страницу

## Цель

Упростить пользовательский путь в функционале "Итоги года 2025", объединив две отдельные страницы (результаты теста и ИИ-план на 2026) в одну целостную страницу.

## Изменённые файлы

### 1. `src/views/land/NewYearResults.vue` — ПЕРЕПИСАН

**Добавлено:**

1. **Loading State (Анимация ожидания)**
   - Спиннер с анимацией вращения
   - Текст "Генерируем твой план на 2026 ✨"
   - 3-шаговый прогресс:
     - Шаг 1: "Анализируем твои ответы"
     - Шаг 2: "Подбираем цели"
     - Шаг 3: "Составляем план действий"
   - Каждый шаг подсвечивается при активации и получает галочку при завершении

2. **ИИ-генерация плана**
   - Функция `generatePlan()` — асинхронный вызов API `/api/ai/year-plan`
   - Передаваемые данные: `sphereScores`, `growthZones`, `answers`
   - Fallback на демо-план при ошибке API
   - Демо-план содержит 3 цели (финансы, здоровье, карьера) с шагами и 4-недельный план

3. **Confetti-эффект**
   - Импорт `canvas-confetti`
   - Функция `fireConfetti()` — запускает цветное конфетти с двух сторон экрана
   - Срабатывает через 300мс после загрузки плана

4. **План на 2026 (секция plan-section)**
   - Hero-блок с мотивационным текстом
   - Цели в виде раскрывающихся карточек:
     - Номер цели
     - Сфера жизни (иконка + название)
     - Название цели
     - Метрика успеха
     - Шаги к цели (при раскрытии)
   - План на январь — 4 недели с датами и задачами

5. **CTA-блок**
   - Заголовок: "🚀 Сохрани план в приложении"
   - Подпись: "Все X шагов автоматически появятся в твоём планировщике с напоминаниями"
   - Зелёная кнопка с пульсацией: "Добавить в OnePercent →"
   - Хинт: "Бесплатно • Telegram-бот напомнит о задачах"
   - Ссылка ведёт на `/auth/register`

**Удалено:**
- Статические рекомендации на основе `store.recommendations`
- Табы зон роста с целями/шагами/недельным планом
- Секция `service-flow-section` с мокапом планировщика
- Ссылки на `/land/newyear/plan`

**Изменено:**
- Структура template: добавлен `v-if="loading"` / `v-else` для переключения состояний
- Script setup: добавлены переменные `loading`, `loadingStep`, `loadingMessage`, `plan`, `expandedGoal`
- Добавлены вспомогательные функции: `getSphereIcon()`, `getSphereName()`, `getTotalHours()`, `getWeekDates()`, `toggleGoal()`
- onMounted теперь вызывает `generatePlan()` после проверки `store.isCompleted`

### 2. `src/views/land/NewYearPlan.vue` — УДАЛЁН

Файл полностью удалён, так как вся его функциональность перенесена в NewYearResults.vue.

### 3. `src/router/index.js` — ИЗМЕНЁН

**Удалено:**
```javascript
const NewYearPlan = () => import('@/views/land/NewYearPlan.vue')
```

```javascript
{
  path: '/land/newyear/plan',
  name: 'newyear-plan',
  component: NewYearPlan,
  meta: { title: 'Твой план на 2026 — OnePercent', public: true }
},
```

**Изменено:**
- Мета-тег для `/land/newyear/results`: `'Твои итоги 2025 и план на 2026 — OnePercent'`

### 4. `replit.md` — ОБНОВЛЁН

Добавлено описание функционала Year Review 2025:
```
-   **Year Review 2025 (Итоги года)**: Single-page flow at `/land/newyear` with test → results workflow. Results page now includes AI-generated 2026 plan with loading animation, confetti effect, and CTA block for app registration. AI plan generation uses `/api/ai/year-plan` with demo fallback.
```

## Пользовательский путь (до и после)

### До изменений:
1. `/land/newyear` — лендинг
2. `/land/newyear/test` — прохождение теста
3. `/land/newyear/results` — результаты (баллы по сферам, сильные стороны, зоны роста)
4. `/land/newyear/plan` — отдельная страница с ИИ-планом (требовала клик на CTA)

### После изменений:
1. `/land/newyear` — лендинг
2. `/land/newyear/test` — прохождение теста
3. `/land/newyear/results` — **всё на одной странице:**
   - Результаты теста
   - Автоматическая генерация ИИ-плана (с анимацией)
   - Конфетти при готовности
   - CTA для регистрации

## Технические детали

### API вызов
```javascript
const response = await fetch('/api/ai/year-plan', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    sphereScores: store.sphereScores,
    growthZones: store.growthZones.map(z => ({ id: z.id, name: z.name })),
    answers: store.answers
  })
})
```

### Демо-план (fallback)
```javascript
const demoPlan = {
  motivation: '...',
  goals: [
    { id: 'goal-1', title: '...', sphereId: 'welfare', metric: '...', steps: [...] },
    { id: 'goal-2', title: '...', sphereId: 'health', metric: '...', steps: [...] },
    { id: 'goal-3', title: '...', sphereId: 'work', metric: '...', steps: [...] }
  ],
  weekPlan: [
    { week: 1, focus: '...', tasks: [...] },
    { week: 2, focus: '...', tasks: [...] },
    { week: 3, focus: '...', tasks: [...] },
    { week: 4, focus: '...', tasks: [...] }
  ]
}
```

### Анимация загрузки (тайминги)
- Шаг 1 → Шаг 2: 1500мс
- Шаг 2 → Шаг 3: 3000мс (с начала)
- Конфетти: 300мс после загрузки плана

## Результат

- Упрощён пользовательский путь (на 1 шаг меньше)
- Пользователь сразу видит свой персонализированный план после теста
- Улучшен UX за счёт анимации ожидания и конфетти
- Конверсия в регистрацию должна вырасти (CTA показывается в контексте готового плана)
