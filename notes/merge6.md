# Merge 6 - SSP Intro Page Hero Icon Circle

**Дата:** 28 ноября 2025

## Обзор сессии

В этой сессии была реализована стилизация иконки на вводной странице модуля ССП (Система сбалансированных показателей) — добавлен большой круг с полупрозрачным фоном для иконки ChartPie, по образцу дизайна с галочкой в зелёном круге.

---

## Выполненные изменения

### 1. Обновлена HTML-разметка иконки

**Файл:** `src/views/BalancedScorecard.vue`

**Было (строки 6-8):**
```vue
<div class="icon-wrapper lg primary">
  <ChartPie :size="32" />
</div>
```

**Стало:**
```vue
<div class="hero-icon-circle">
  <ChartPie :size="48" :stroke-width="1.5" />
</div>
```

**Изменения:**
- Заменён класс `icon-wrapper lg primary` на новый `hero-icon-circle`
- Размер иконки увеличен с 32px до 48px
- Добавлен параметр `stroke-width="1.5"` для более тонкого контура

---

### 2. Добавлен CSS класс .hero-icon-circle

**Файл:** `src/views/BalancedScorecard.vue` (секция `<style scoped>`)

**Добавлено после `.empty-state-card .empty-icon` (строки 824-834):**
```css
.hero-icon-circle {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  background: rgba(99, 102, 241, 0.12);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 1.5rem;
  color: var(--primary-color);
}
```

**Характеристики стиля:**
| Свойство | Значение | Назначение |
|----------|----------|------------|
| `width/height` | 100px | Большой круг для визуального акцента |
| `border-radius` | 50% | Идеальный круг |
| `background` | rgba(99, 102, 241, 0.12) | Полупрозрачный фиолетовый (12% opacity от primary) |
| `display: flex` | center/center | Центрирование иконки внутри круга |
| `margin` | 0 auto 1.5rem | Центрирование и отступ снизу |
| `color` | var(--primary-color) | Цвет иконки из темы |

---

## Дизайн-система

### Паттерн Hero Icon Circle

Этот паттерн используется для визуального акцента на вводных страницах модулей:

```
┌─────────────────────────────┐
│                             │
│       ┌───────────┐         │
│       │           │         │
│       │   🎯 48px │  100px  │
│       │           │         │
│       └───────────┘         │
│                             │
│      Заголовок модуля       │
│                             │
│        Подзаголовок         │
│                             │
└─────────────────────────────┘
```

**Цвета фона по модулям (предложение):**
- ССП: `rgba(99, 102, 241, 0.12)` — фиолетовый (primary)
- Банк целей: `rgba(34, 197, 94, 0.12)` — зелёный (success)
- Декомпозиция: `rgba(249, 115, 22, 0.12)` — оранжевый
- Планирование: `rgba(59, 130, 246, 0.12)` — синий

---

## Визуальное сравнение

### До изменений
- Маленькая иконка 32px в компактном круге
- Класс `icon-wrapper lg` — стандартный размер

### После изменений
- Большая иконка 48px в круге 100x100px
- Мягкий полупрозрачный фон 12% opacity
- Тонкий контур (stroke-width: 1.5)
- Визуально соответствует референсу (галочка в зелёном круге)

---

## Файлы изменённые в этой сессии

| Файл | Изменения |
|------|-----------|
| `src/views/BalancedScorecard.vue` | Обновлена разметка иконки (строки 6-8), добавлен CSS класс `.hero-icon-circle` (строки 824-834) |

---

## Технические заметки

### Выбор цвета фона

Использован `rgba(99, 102, 241, 0.12)` вместо `var(--primary-color)` с opacity по причине:
- CSS не поддерживает opacity для CSS-переменных напрямую
- Альтернатива: `color-mix(in srgb, var(--primary-color) 12%, transparent)` — менее совместима с браузерами
- Хардкод RGB значения обеспечивает консистентность

### Почему stroke-width: 1.5

При увеличении иконки с 32px до 48px пропорционально увеличивается и толщина линий. Параметр `stroke-width="1.5"` делает контур тоньше и элегантнее для большого размера.

---

## Статус

✅ Иконка отображается в большом круге с мягким фоном
✅ Размер увеличен до 48px
✅ Стиль соответствует референсному дизайну
✅ Приложение работает корректно
