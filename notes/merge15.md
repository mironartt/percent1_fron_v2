# Merge 15: Мобильная оптимизация GoalsBank

**Дата:** 4 декабря 2025

## Обзор изменений

Комплексная оптимизация мобильной версии страницы "Банк целей" (GoalsBank.vue) для улучшения UX на мобильных устройствах.

---

## 1. Упрощение карточек статистики

**Файл:** `src/views/GoalsBank.vue`

### Что сделано:
- На мобильных (≤768px) показываются только 2 карточки статистики вместо 4
- Скрыты: "Идей в банке", "Ложных целей"
- Оставлены: "Истинных целей", "Целей в работе"

### CSS классы:
```css
.hide-mobile { display: none; } /* на ≤768px */
```

---

## 2. Dropdown меню действий (Action Sheet)

**Файл:** `src/views/GoalsBank.vue`

### Проблема:
- На мобильных слишком много кнопок действий в карточке цели
- Dropdown обрезался границами карточки

### Решение:
- Реализован Action Sheet паттерн (меню снизу экрана)
- Используется `<Teleport to="body">` для вывода меню за пределы карточки
- Добавлен затемнённый overlay для закрытия при клике вне меню
- Кнопка "Отмена" для явного закрытия

### Структура:
```
Desktop: все кнопки inline
Mobile: основная кнопка + кнопка "..." → Action Sheet
```

### Action Sheet содержит:
- Редактировать
- Декомпозировать (если цель validated)
- Убрать из работы (если цель в работе)
- Отмена

### CSS стили:
```css
.dropdown-overlay {
  position: fixed;
  background: rgba(0, 0, 0, 0.4);
  z-index: 9998;
}

.dropdown-menu-mobile {
  position: fixed;
  left: 0.75rem;
  right: 0.75rem;
  bottom: 0.75rem;
  border-radius: 16px;
  z-index: 9999;
  animation: slideUpSheet 0.25s ease-out;
}
```

---

## 3. Перенос бейджа сферы

**Файл:** `src/views/GoalsBank.vue`

### Было:
```
[В работе]
пкуп
[Благосостояние ⚠]
пкуп
[кнопки]
```

### Стало:
```
[В работе]  [Благосостояние ⚠]
пкуп
пкуп
[кнопки]
```

### Реализация:
- Добавлен `<div class="status-row">` для группировки статуса и сферы
- На мобильных: сфера рядом со статусом (класс `mobile-only`)
- На десктопе: сфера остаётся под названием (класс `desktop-only`)

### CSS:
```css
.goals-table-section .status-row {
  display: flex;
  align-items: stretch;
  gap: 0.5rem;
}

.goals-table-section .status-row .status-badge,
.goals-table-section .status-row .goal-sphere-badge-new {
  display: inline-flex;
  align-items: center;
  padding: 0.375rem 0.75rem;
  font-size: 0.8125rem;
  line-height: 1;
}
```

---

## 4. Выравнивание бейджей статуса и сферы

**Файл:** `src/views/GoalsBank.vue`

### Проблема:
- Бейджи статуса и сферы были разной высоты
- Визуально не на одном уровне

### Решение:
- Унифицированы padding, font-size, line-height
- Добавлен `align-items: stretch` для row
- Объединены стили в один селектор

---

## 5. Кнопки действий в правый угол

**Файл:** `src/views/GoalsBank.vue`

### Изменение:
```css
/* Было */
justify-content: flex-start;

/* Стало */
justify-content: flex-end;
```

Кнопки теперь справа — удобнее для правой руки.

---

## 6. Унификация кнопок внизу страницы

**Файл:** `src/views/GoalsBank.vue`

### Было:
- "Запланировать задачу" — обычный размер
- "Добавить новую цель" — увеличенный (btn-lg)

### Стало:
- Обе кнопки одинакового размера
- На мобильных: вертикальное расположение, 100% ширина

### CSS:
```css
.summary-actions {
  flex-direction: column;
  gap: 0.75rem;
}

.summary-actions .btn {
  width: 100%;
  justify-content: center;
  padding: 0.75rem 1rem;
  font-size: 0.9375rem;
}
```

---

## 7. Мелкие правки

### Изменение текста:
- "Цель / Идея" → "Цель" в заголовке таблицы

### Иконки:
- Calendar иконка уменьшена с 18 до 16px для консистентности

---

## Итоговая структура мобильной карточки цели

```
┌─────────────────────────────────────┐
│ [В работе]  [📊 Благосостояние ⚠]   │  ← статус + сфера
│                                     │
│ Название цели                       │  ← текст цели
│                                     │
│ Почему это важно                    │  ← описание
│─────────────────────────────────────│
│                          [✓] [...]  │  ← кнопки справа
└─────────────────────────────────────┘
```

---

## Технические детали

### Новые CSS классы:
- `.status-row` — контейнер для статуса и сферы
- `.dropdown-overlay` — затемнение для Action Sheet
- `.dropdown-menu-mobile` — стили Action Sheet
- `.dropdown-item-cancel` — кнопка отмены

### Новые JS refs/функции:
- `openActionsDropdown` — ref для отслеживания открытого dropdown
- `toggleActionsDropdown(goalId)` — открыть/закрыть
- `closeActionsDropdown()` — закрыть

### Импорты:
- Добавлен `MoreVertical` из lucide-vue-next

---

## Файлы изменены

1. `src/views/GoalsBank.vue` — основные изменения мобильной версии
