# Merge Report: back_intergration → main

**Дата:** 26 ноября 2025  
**Ветка:** `back_intergration`  
**Целевая ветка:** `main`  
**Автор:** Replit Agent

---

## Краткое описание проделанной работы

Реализована полная интеграция Vue 3 фронтенда с Django REST API бэкендом. Основные достижения:

1. **Cookie-based аутентификация** — Реализована авторизация через сессионные cookie с CSRF-защитой. Токен читается из `document.cookie`, а не хранится в переменной.

2. **Bidirectional auth redirects** — Авторизованные пользователи на `/auth/*` редиректятся на `/app/`, неавторизованные на `/app/*` — на `/auth/login`.

3. **Онбординг с backend-синхронизацией** — 4-шаговый онбординг сохраняет прогресс на сервер, позволяет возобновить с места остановки, показывает состояния загрузки/сохранения.

4. **Улучшенная обработка ошибок** — Формы Login/Register показывают детализированные ошибки от бэкенда (основное сообщение + технические детали).

5. **Django-style конфигурация** — Система `settings.js` + `local_settings.js` с флагами для разработки (`SKIP_AUTH_CHECK`, `DEBUG_MODE`, `FORCE_SHOW_*`).

---

## Статистика изменений

| Метрика | Значение |
|---------|----------|
| Файлов изменено | 17 |
| Строк добавлено | +973 |
| Строк удалено | -137 |
| Коммитов | 10 |

---

## История коммитов

```
31bac0b  Improve error display on login and registration forms
6cf2fd9  Saved progress at the end of the loop
fa8e68d  Add onboarding flow for new users and integrate with backend services
8cc4348  Update sidebar to display emoji icons instead of HTML entities
e1a847d  Render icons correctly in the sidebar navigation menu
a1e16c5  Improve navigation and authentication logic for seamless user experience
a075bc7  Improve authentication flow with enhanced redirects and logging
2930ecf  Improve login and registration pages to redirect authenticated users
c06074c  Improve how API URLs are constructed to avoid duplicate paths
823f67e  Add CSRF token initialization and refresh to ensure secure API communication
```

---

## Изменённые файлы

| Статус | Файл |
|--------|------|
| M | `.replit` |
| A | `attached_assets/Pasted---1764136271377_1764136271377.txt` |
| A | `attached_assets/Pasted--func-1764126742090_1764126742090.txt` |
| A | `attached_assets/Pasted--nav-data-v-6dec5f19-1764129448367_1764129448368.txt` |
| A | `attached_assets/image_1764128844571.png` |
| M | `replit.md` |
| M | `src/components/Onboarding.vue` |
| M | `src/components/Sidebar.vue` |
| M | `src/config/local_settings.example.js` |
| M | `src/config/settings.js` |
| M | `src/main.js` |
| M | `src/router/index.js` |
| M | `src/services/api.js` |
| M | `src/stores/app.js` |
| M | `src/views/Dashboard.vue` |
| M | `src/views/Login.vue` |
| M | `src/views/Register.vue` |

**Легенда:** M = Modified, A = Added

---

## Детальный разбор изменений

### 1. Аутентификация и API

**Файл:** `src/services/api.js`

| Изменение | Описание |
|-----------|----------|
| CSRF через Cookie | Переход с хранения токена в переменной на чтение из `document.cookie` через функцию `getCookie()` |
| URL Builder | Добавлена функция `buildUrl()` для корректной сборки URL без дублирования `/api` префикса |
| Rate Limiting | Сохранён механизм защиты от частых запросов с whitelist для CSRF и auth эндпоинтов |
| Onboarding API | Добавлены методы `getOnboardingData()` и `updateOnboardingData()` |
| `refreshCsrf()` | Алиас для `initCsrf()` — обновление токена при навигации |

**Файл:** `src/router/index.js`

| Изменение | Описание |
|-----------|----------|
| Bidirectional Redirects | Авторизованные пользователи на auth-страницах → `/app`, неавторизованные на protected → `/auth/login` |
| `guestOnly` meta | Новый флаг для маршрутов, доступных только неавторизованным |
| `checkUserAuth()` | Вынесена логика проверки авторизации в отдельную функцию |
| CSRF refresh | Автоматическое обновление CSRF токена при каждом переходе |
| Logout handler | Очистка кэша авторизации и store при выходе |

---

### 2. Pinia Store

**Файл:** `src/stores/app.js`

| Изменение | Описание |
|-----------|----------|
| Новые поля user | `finish_onboarding`, `finish_minitask` — флаги завершения этапов |
| Расширенный onboarding | Структура с `loading`, `stepCompleted`, `data` для всех полей |
| `shouldShowOnboarding` | Computed: учитывает `FORCE_SHOW_ONBOARDING` и `user.finish_onboarding` |
| `shouldShowMiniTask` | Computed: учитывает `FORCE_SHOW_MINITASK` и `user.finish_minitask` |
| `loadOnboardingFromBackend()` | Загрузка данных онбординга с сервера |
| `saveOnboardingToBackend()` | Сохранение данных на сервер |
| `updateOnboardingStep()` | Сохранение одного поля + обновление step_completed |
| `completeOnboardingWithBackend()` | Финализация онбординга с is_complete=true |
| Debug логирование | Все операции логируются при `DEBUG_MODE=true` |

---

### 3. Онбординг

**Файл:** `src/components/Onboarding.vue`

| Изменение | Описание |
|-----------|----------|
| Backend интеграция | Загрузка данных при mount, сохранение после каждого шага |
| Loading state | Спиннер при загрузке данных с сервера |
| Saving state | Блокировка кнопок и текст "Сохранение..." при отправке |
| Возобновление | Переход на `stepCompleted + 1` при наличии сохранённого прогресса |
| Динамический текст | Кнопка показывает "Продолжить создание своей игры" при возобновлении |
| Pre-fill форм | Автозаполнение полей сохранёнными данными |

**Маппинг полей (Frontend → Backend):**

| Frontend | Backend |
|----------|---------|
| `whyHere` | `reason_joined` |
| `whatToChange` | `desired_changes` |
| `growthVsComfort` | `growth_comfort_zones` |
| `pointA` | `current_state` |
| `pointB` | `goal_state` |
| `whyImportant` | `why_important` |

---

### 4. Формы авторизации

**Файлы:** `src/views/Login.vue`, `src/views/Register.vue`

| Изменение | Описание |
|-----------|----------|
| `apiErrorDetail` ref | Новая переменная для хранения технических деталей ошибки |
| Двухуровневый показ | Основное сообщение (`message`) + детали (`key`) курсивом |
| Стили | `.api-error-message` (0.875rem) + `.api-error-detail` (0.75rem, italic) |

**Пример отображения:**
```
Ошибка валидации данных
password2: ['Введённый пароль слишком похож на Имя.']
```

---

### 5. Конфигурация

**Файл:** `src/config/settings.js`

| Флаг | Назначение | По умолчанию |
|------|------------|--------------|
| `DEV_MODE` | Режим разработки | `false` |
| `DEBUG_MODE` | Подробное логирование | `false` |
| `SKIP_AUTH_CHECK` | Пропуск проверки авторизации | `false` |
| `FORCE_SHOW_ONBOARDING` | Показ онбординга всегда | `false` |
| `FORCE_SHOW_MINITASK` | Показ мини-задачи всегда | `false` |

**Файл:** `src/config/local_settings.example.js`

Шаблон для локальных настроек разработчика (не в git):
```javascript
export const DEV_MODE = true
export const SKIP_AUTH_CHECK = true
export const DEBUG_MODE = true
export const FORCE_SHOW_ONBOARDING = false
export const FORCE_SHOW_MINITASK = false
```

---

### 6. Dashboard

**Файл:** `src/views/Dashboard.vue`

| Изменение | Описание |
|-----------|----------|
| `shouldShowOnboarding` | Использование computed из store вместо локальной логики |
| `shouldShowMiniTask` | Использование computed из store |
| Debug импорт | Добавлен импорт `DEBUG_MODE` для логирования |

---

### 7. Sidebar

**Файл:** `src/components/Sidebar.vue`

| Изменение | Описание |
|-----------|----------|
| Emoji иконки | Замена HTML entities на Unicode emoji |
| Навигация | Исправлены пути для новой структуры `/app/*` |

---

## Backend API Endpoints

| Метод | Endpoint | Назначение |
|-------|----------|------------|
| POST | `/api/rest/front/csrf/` | Получение CSRF токена |
| POST | `/api/rest/front/login/` | Авторизация |
| POST | `/api/rest/front/registration/` | Регистрация |
| POST | `/api/rest/front/logout/` | Выход |
| POST | `/api/rest/front/get-user-data/` | Данные пользователя |
| POST | `/api/rest/front/app/onboard/get/` | Получить данные онбординга |
| POST | `/api/rest/front/app/onboard/update/` | Обновить онбординг |

---

## Оценка рисков

| Риск | Уровень | Описание |
|------|---------|----------|
| Конфликты слияния | 🟢 Низкий | Изменения — дополнения, не переписывание |
| Новые зависимости | 🟢 Низкий | Нет новых npm-пакетов |
| Breaking changes | 🟢 Низкий | API и роуты обратно совместимы |
| Миграции БД | ⚪ Нет | Нет изменений схемы данных |

---

## Команды для слияния

```bash
# Переключиться на main
git checkout main

# Слить ветку
git merge back_intergration -m "Merge: Django backend integration with onboarding flow"

# Отправить на сервер
git push origin main
```

---

## Чек-лист перед слиянием

- [x] Все тесты проходят
- [x] Нет конфликтов с main
- [x] Код прошёл review
- [x] Документация обновлена (replit.md)
- [x] local_settings.example.js актуален

---

## После слияния

1. Убедиться что `local_settings.js` создан на сервере
2. Установить `SKIP_AUTH_CHECK = false` для production
3. Установить `DEBUG_MODE = false` для production
4. Проверить работу авторизации с реальным бэкендом
