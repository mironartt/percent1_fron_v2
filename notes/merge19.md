# Merge 19 - Planning UI + Marketing Landing Page

**Дата:** 7 декабря 2025

## Обзор сессии

В этой сессии были выполнены две основные задачи:
1. Улучшение UI модуля Планирования (карточки шагов, фильтры, приоритеты)
2. Создание маркетингового лендинга на основе procent1.ru

---

## Часть 1: Улучшения модуля Планирования

### 1.1 Исправление локального сохранения приоритета/времени

**Проблема:** При изменении приоритета или времени шага в DEV_MODE изменения не отображались, потому что `updateStepPriority` и `updateStepTime` только отправляли запрос в API без локального обновления.

**Решение:** Добавлена функция `updateStepInLocalPlan()` для мгновенного обновления данных в `store.weeklyPlans`:

```javascript
function updateStepInLocalPlan(goal, step, updates) {
  const weekStart = weekDays.value[0]?.date
  if (!weekStart) return
  
  const goalId = goal.backendId || goal.id
  const stepId = step.backendId || step.id
  
  let plan = store.weeklyPlans.find(p => p.weekStart === weekStart)
  if (!plan) {
    plan = { weekStart, scheduledTasks: [] }
    store.weeklyPlans.push(plan)
  }
  
  const task = plan.scheduledTasks.find(
    t => (t.goalId === goalId || t.goalId === goal.id) && 
         (t.stepId === stepId || t.stepId === step.id)
  )
  
  if (task) {
    if (updates.priority !== undefined) task.priority = updates.priority
    if (updates.timeEstimate !== undefined) task.timeEstimate = updates.timeEstimate
    if (updates.completed !== undefined) task.completed = updates.completed
  }
  
  store.saveToLocalStorage()
}
```

**Изменённые функции:**
- `updateStepPriority()` — теперь сначала вызывает `updateStepInLocalPlan()`, затем API
- `updateStepTime()` — аналогично

**Файл:** `src/views/Planning.vue`

---

### 1.2 Объединение фильтров в одну строку

**Было:** Два уровня фильтров:
- Фильтр по сферам (отдельная строка)
- Фильтр по статусу (отдельная строка)

**Стало:** Единая строка `.filters-row` с разделителем:
```
[Все сферы 0] [Сфера1] [Сфера2] | [Все] [Незапл.] [Запл.]
```

**Изменения в шаблоне:**
- Заменён `<div class="filters-section">` на `<div class="filters-row">`
- Убраны вложенные контейнеры `chip-filters` и `status-chips`
- Добавлен визуальный разделитель `<span class="filter-divider">|</span>`
- Сокращены названия статусов: "Незапланированные" → "Незапл.", "Запланированные" → "Запл."

**Новые CSS классы:**
```css
.filters-row {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  flex-wrap: wrap;
  padding: 0.5rem 0;
  margin-bottom: 1rem;
}

.filter-divider {
  color: var(--border-color, #d1d5db);
  font-weight: 300;
  margin: 0 0.125rem;
}

.sphere-chip { padding: 0.375rem 0.625rem; font-size: 0.8125rem; }
.status-chip { padding: 0.375rem 0.625rem; font-size: 0.75rem; }
```

**Файл:** `src/views/Planning.vue`

---

### 1.3 Кнопка перехода в декомпозицию

**Добавлено:** Кнопка "+" рядом с каждой целью в блоке "Цели и шаги" для быстрого перехода к добавлению шагов.

**Шаблон:**
```html
<button 
  class="decompose-btn" 
  @click.stop="goToDecomposition(goal)"
  title="Добавить шаги"
>
  <Plus :size="16" />
</button>
```

**Стили:**
```css
.decompose-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px;
  height: 28px;
  border: 1px solid var(--border-color, #e5e7eb);
  border-radius: 8px;
  background: var(--bg);
  color: var(--primary, #6366f1);
  cursor: pointer;
  transition: all 0.2s;
}

.decompose-btn:hover {
  background: var(--primary, #6366f1);
  color: white;
  border-color: var(--primary, #6366f1);
}
```

**Файл:** `src/views/Planning.vue`

---

### 1.4 Улучшения карточек шагов (предыдущая сессия, закреплено)

- **Priority-stripe:** Цвет для пустого приоритета изменён на `#c7d2fe` (светло-фиолетовый)
- **Плейсхолдеры:** Время показывается как "—" если не задано, приоритет как "○"
- **Компактный layout:** `padding: 0.5rem 0.625rem`, `border: 1px solid`, `min-height: 44px`
- **Иконки:** Calendar и Clock уменьшены до 11px

---

## Часть 2: Маркетинговый лендинг

### 2.1 Структура лендинга

Создан полноценный маркетинговый лендинг на основе procent1.ru с адаптацией под веб-приложение.

**Маршрут:** `/` (главная страница)

**Секции:**

1. **Hero** — Заголовок "Системный рост в жизни через простые действия", подзаголовок, CTA "Сделать +1% уже сегодня"

2. **Эффект 1%** — Интерактивный слайдер 30-365 дней:
   - Расчёт множителя по формуле `1.01^days`
   - Динамическое описание результата (30 дней → 90 → 180 → 365)

3. **Проблема → Решение** — Тёмная секция с философией "Проблема не в мотивации, а в отсутствии системы"

4. **Функции приложения** — 6 карточек:
   - 🎯 Колесо баланса (ССП)
   - 🏦 Банк целей
   - 📅 Недельный планировщик
   - 🤖 AI Mentor
   - 📓 Дневник рефлексии
   - ⭐ Геймификация

5. **Путь One Percent** — 5 шагов с нумерацией:
   - 01: Диагностика → Осознание
   - 02: Осознание → Фокус
   - 03: Фокус → Системность
   - 04: Системность → Энергия
   - 05: Энергия → Рост

6. **Кому подойдёт** — 3 карточки аудитории (предприниматели, фрилансеры, менеджеры)

7. **Отзывы** — 4 цитаты участников

8. **Преимущества** — 5 чипов (фокус, дисциплина, приоритеты, рост без стресса, уверенность)

9. **Финальный CTA** — Градиентная секция "Твоя жизнь не нуждается в революции. Ей нужен +1% каждый день"

10. **Footer** — Лого, ссылки, копирайт

**Файл:** `src/views/Landing.vue`

---

### 2.2 Исправление App.vue

**Проблема:** Лендинг отображался внутри основного layout с сайдбаром.

**Решение:** Добавлена проверка `isLandingPage` в `hasSidebar`:

```javascript
const isLandingPage = computed(() => {
  return route.name === 'landing' || route.path === '/'
})

const hasSidebar = computed(() => 
  !isAuthPage.value && !isOnboarding.value && !isLandingPage.value
)
```

**Файл:** `src/App.vue`

---

### 2.3 Интерактивный слайдер эффекта 1%

```javascript
const days = ref(90)

const multiplier = computed(() => {
  return Math.pow(1.01, days.value).toFixed(2)
})
```

```html
<input type="range" min="30" max="365" v-model="days" class="days-slider">
<div class="multiplier">× {{ multiplier }}</div>
```

---

## Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `src/views/Planning.vue` | Локальное сохранение приоритета/времени, объединение фильтров, кнопка декомпозиции |
| `src/views/Landing.vue` | Полная переработка маркетингового лендинга |
| `src/App.vue` | Исключение лендинга из layout с сайдбаром |
| `replit.md` | Добавлено описание Marketing Landing Page |

---

## Технические детали

### Паттерн локального обновления
```
1. Обновить локальный store (updateStepInLocalPlan)
2. Закрыть UI (closeBottomSheet)
3. Отправить запрос в API (асинхронно)
4. При успехе — обновить данные с сервера (loadWeeklySteps)
5. При ошибке — залогировать (данные уже сохранены локально)
```

### CSS переменные лендинга
- Основной градиент: `linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%)`
- Фон: `#fafafa`
- Текст: `#1a1a2e`
- Вторичный текст: `#6b7280`

---

## Статус

Все задачи выполнены. Лендинг готов к использованию.
