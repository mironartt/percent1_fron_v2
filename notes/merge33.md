# Merge 33: Замена Landing Version 4 на новый дизайн

**Дата:** 25 декабря 2025  
**Статус:** Завершено

## Обзор

Полная замена существующего лендинга `/land/version4` на новый комплексный 8-блочный дизайн, демонстрирующий полный путь пользователя от диагностики до результата.

## Исходные файлы

- **Источник:** ZIP-архив `attached_assets/files_(2)_1766680980788.zip`
- **Содержимое архива:**
  - `index.html` — HTML-разметка (771 строка)
  - `styles.css` — CSS-стили (1884 строки)
  - `script.js` — JavaScript-логика (45 строк)

## Внесённые изменения

### 1. Удаление старого компонента

- **Файл:** `src/views/land/LandingVersion4.vue`
- **Действие:** Полное удаление старого компонента

### 2. Создание нового Vue-компонента

- **Файл:** `src/views/land/LandingVersion4.vue`
- **Размер:** ~2218 строк
- **Структура:**
  - `<template>` — HTML-разметка всех 8 блоков
  - `<script setup>` — Vue Composition API логика
  - `<style scoped>` — CSS-стили с CSS-переменными

### 3. Конвертация HTML в Vue Template

Преобразованы следующие секции:

| Блок | Название | Описание |
|------|----------|----------|
| Hero | Герой | Заголовок, подзаголовок, CTA, социальное доказательство |
| Block 1 | Колесо жизни | Диагностика по 6 сферам с оценками |
| Block 2 | Цели | AI-генерируемые цели из диагностики |
| Block 3 | Декомпозиция | План на первую неделю с 4 шагами |
| Block 4 | Планирование | Календарь с задачами на неделю |
| Block 5 | Привычки | Трекер привычек со стриками и XP |
| Block 6 | Награды | Система наград с балансом XP |
| Block 7 | AI Ментор | Telegram-бот с расписанием дня |
| Final CTA | Финальный призыв | Чеклист + кнопка действия |
| Footer | Подвал | Логотип, ссылки, копирайт |

### 4. Адаптация JavaScript под Vue Composition API

```javascript
// Реактивные данные
const socialProofCount = ref(2847)
const showBackToTop = ref(false)
const accordions = ref({ howItWorks: false })

// Массивы данных
const spheres = ref([...])      // 6 сфер жизни
const steps = ref([...])        // 4 шага плана
const calendarDays = ref([...]) // 7 дней недели
const habits = ref([...])       // 3 привычки
const rewards = ref([...])      // 4 награды
const schedule = ref([...])     // 4 времени дня

// Функции
function scrollTo(selector) {...}      // Плавная прокрутка
function scrollToTop() {...}           // Наверх
function toggleAccordion(name) {...}   // Аккордеон
function getScoreClass(score) {...}    // CSS-класс оценки
function getBadgeClass(score) {...}    // CSS-класс бейджа

// Lifecycle hooks
onMounted() {...}   // Добавление scroll listener
onUnmounted() {...} // Удаление scroll listener
```

### 5. Интеграция CSS-стилей

#### Исправление CSS-переменных

**Проблема:** CSS-переменные в `:root` не работают со scoped-стилями Vue (Vue добавляет `[data-v-xxxx]`, и `:root[data-v-xxxx]` не совпадает с `<html>`).

**Решение:** Перенос CSS-переменных из `:root` в `.landing-v4`:

```css
/* Было (не работает со scoped) */
<style scoped>
:root {
  --primary: #6366F1;
  ...
}

/* Стало (работает со scoped) */
<style scoped>
.landing-v4 {
  --primary: #6366F1;
  --primary-hover: #5558E3;
  --primary-light: #E0E7FF;
  --bg-main: #F8F9FA;
  --bg-white: #FFFFFF;
  --bg-light: #F3F4F6;
  --text-primary: #1F2937;
  --text-secondary: #6B7280;
  --text-muted: #9CA3AF;
  --success: #10B981;
  --warning: #F59E0B;
  --error: #EF4444;
  ...
}
```

#### CSS-компоненты

Включены стили для:

- **Layout:** Container, grid layouts, flexbox
- **Typography:** h1-h6, paragraphs, links
- **Buttons:** Primary, large, with hover effects
- **Cards:** Goal cards, stat cards, reward items
- **Forms:** Accordions, toggles
- **Visualizations:** Wheel chart (SVG), progress bars, calendars
- **Mockups:** App mockup, phone frame, Telegram chat
- **Interactive:** Hover states, transitions, animations

#### Responsive Design

```css
@media (max-width: 1024px) {
  /* Одноколоночный layout */
  .hero-content, .block-content {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 768px) {
  /* Мобильная адаптация */
  .hero-meta { flex-direction: column; }
  .day-task { display: none; }
  .phone-mockup { max-width: 250px; }
}

@media (max-width: 480px) {
  /* Маленькие экраны */
  .btn { width: 100%; }
  .cal-day { font-size: 0.75rem; }
}
```

## Интерактивные функции

### 1. Плавная прокрутка

Все кнопки "Продолжить →" используют `@click.prevent="scrollTo('#blockN')"` для плавного перехода между секциями с учётом offset 80px.

### 2. Аккордеон

Раздел "Как AI формирует цели?" с анимированным раскрытием/скрытием контента и поворотом иконки стрелки.

### 3. Кнопка "Наверх"

Появляется при прокрутке более 500px, плавно прокручивает страницу к началу.

### 4. Анимации

- Float-анимация для app mockup
- Hover-эффекты для карточек целей и привычек
- Transition-анимации для всех интерактивных элементов

## Данные компонента

### Сферы жизни (spheres)
```javascript
[
  { name: 'Здоровье', score: 4, icon: '❤️', label: 'Критическая зона' },
  { name: 'Карьера', score: 8, icon: '💼', label: 'Сильная сторона' },
  { name: 'Семья', score: 6, icon: '👨‍👩‍👧', label: 'Норма' },
  { name: 'Благосостояние', score: 5, icon: '💰', label: 'Требует внимания' },
  { name: 'Хобби и отдых', score: 7, icon: '🎨', label: 'Норма' },
  { name: 'Дружба', score: 3, icon: '👥', label: 'Требует внимания' }
]
```

### Привычки (habits)
```javascript
[
  { icon: '🏃', title: 'Утренняя зарядка', streak: 14, xp: 15 },
  { icon: '💧', title: 'Пить воду', streak: 21, xp: 10 },
  { icon: '📚', title: 'Чтение 30 мин', streak: 7, xp: 20 }
]
```

### Награды (rewards)
```javascript
[
  { icon: '🎬', title: 'Вечер кино', cost: 500, status: 'completed' },
  { icon: '🍕', title: 'Любимая пицца', cost: 750, status: 'completed' },
  { icon: '🎮', title: 'Новая игра', cost: 1500, status: 'available' },
  { icon: '✈️', title: 'Поездка на выходные', cost: 5000, status: 'locked', progress: 49 }
]
```

## Маршрутизация

- **Путь:** `/land/version4`
- **Роутер:** Без изменений — маршрут уже существовал

## Коммиты

1. `2cd53c56` — Создание нового компонента LandingVersion4.vue
2. `63d0e641` — Исправление CSS-переменных (`:root` → `.landing-v4`)
3. `b1e6317a` — Финальное сохранение

## Результат

Лендинг успешно развёрнут и функционирует по адресу `/land/version4`:

- Hero-секция с mockup приложения
- Визуализация Колеса жизни (SVG)
- Карточки целей с hover-эффектами
- Календарь недели с задачами
- Трекер привычек со стриками
- Система наград с прогресс-барами
- Mockup Telegram-чата с AI Ментором
- Финальный CTA с чеклистом

## Технические детали

| Параметр | Значение |
|----------|----------|
| Файл компонента | `src/views/land/LandingVersion4.vue` |
| Размер файла | ~2218 строк |
| Template | ~690 строк |
| Script | ~100 строк |
| Styles | ~1400 строк |
| CSS-переменные | 47 переменных |
| Breakpoints | 1024px, 768px, 480px |
