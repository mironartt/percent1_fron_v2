# Merge 28: Telegram Bot Integration

**Дата:** 11 декабря 2025

## Обзор

Реализована полноценная интеграция Telegram бота для приложения OnePercent. Бот дублирует основной функционал веб-приложения и предназначен для повышения вовлечённости и удержания пользователей.

## Созданные файлы

### `server/telegramBot.js`
Основной файл Telegram бота на Node.js с использованием библиотеки Telegraf.

## Технический стек

- **Telegraf** — фреймворк для Telegram Bot API
- **node-cron** — планировщик задач для напоминаний
- **OpenAI** — интеграция AI-ментора (опционально)

## Реализованный функционал

### 1. Команды бота
- `/start` — приветствие, привязка аккаунта через deep link
- `/menu` — показать главное меню

### 2. Главное меню (Reply Keyboard)
| Кнопка | Функция |
|--------|---------|
| 📋 Задачи | Список задач на сегодня с inline-кнопками отметки |
| ✅ Привычки | Список привычек с toggle-кнопками |
| 📝 Дневник | Пошаговый диалог (4 вопроса) |
| 🤖 Ментор | Чат с AI-коучем |
| 📊 Прогресс | Недельная сводка статистики |
| ⚙️ Настройки | Напоминания и часовой пояс |

### 3. Inline-кнопки и callback handlers
- `toggle_task_{id}` — отметка задачи
- `toggle_habit_{id}` — отметка привычки
- `start_journal` — начать заполнение дневника
- `skip_journal` — пропустить дневник
- `show_tasks` / `show_habits` — быстрый доступ из напоминаний
- `settings_reminders` / `settings_timezone` — подменю настроек
- `toggle_morning` / `toggle_evening` / `toggle_streak` — переключатели напоминаний
- `tz_moscow` / `tz_ekb` / `tz_nsk` / `tz_vlad` — выбор часового пояса

### 4. Дневник (Conversation Flow)
Пошаговый диалог с 4 вопросами:
1. Что хорошего произошло сегодня?
2. Чему ты научился?
3. За что ты благодарен?
4. Что можно улучшить завтра?

Использует Map `journalSessions` для хранения состояния диалога.

### 5. AI Ментор
- Интеграция с OpenAI GPT-4o-mini
- Системный промпт для роли коуча
- Graceful fallback на заготовленные мотивационные фразы при недоступности API

### 6. Автоматические напоминания (cron jobs)
| Время | Тип | Описание |
|-------|-----|----------|
| 08:00 MSK | Утреннее | Приветствие + задачи и привычки на день |
| 21:00 MSK | Вечернее | Напоминание заполнить дневник |
| 22:00 MSK | Streak | Предупреждение о потере серии |
| Вс 20:00 MSK | Недельный | Сводный отчёт за неделю |

### 7. Экспортируемые функции
Для вызова из backend при событиях:
```javascript
sendMorningReminder(userId, userName)
sendEveningReminder(userId, userName)
sendStreakWarning(userId, userName, streakDays)
sendXPNotification(userId, amount, reason)
sendAchievementUnlocked(userId, achievementName, achievementIcon)
```

## Переменные окружения

| Переменная | Обязательность | Описание |
|------------|----------------|----------|
| `TELEGRAM_BOT_TOKEN` | Обязательно | Токен бота от @BotFather |
| `WEBAPP_URL` | Опционально | URL веб-приложения для deep links |
| `AI_INTEGRATIONS_OPENAI_API_KEY` | Опционально | Для AI-ментора |

## Workflow

Добавлен workflow `Telegram Bot` с командой:
```
node server/telegramBot.js
```

## Что требует доработки (для backend-разработчика)

### 1. Валидация токенов авторизации
Сейчас deep link просто сохраняет токен. Нужно:
- Эндпоинт `POST /api/telegram/validate-token/`
- Проверка токена на сервере перед привязкой

### 2. Интеграция с реальным API
Заменить demo-данные на запросы:
- `GET /api/habits/` — привычки
- `POST /api/habits/{id}/complete/` — отметка
- `GET /api/planning/steps/?date=today` — задачи
- `POST /api/journal/entries/` — сохранение дневника

### 3. Персистентность настроек
Добавить в модель пользователя:
- `telegram_morning_reminder`
- `telegram_evening_reminder`
- `telegram_streak_alerts`
- `telegram_timezone`

### 4. Telegram Payments (опционально)
Для подписок через бота нужен Provider Token.

## Обновлённая документация

Файл `replit.md` дополнен разделом "Telegram Bot Integration" с описанием всех функций, переменных окружения и pending features.
