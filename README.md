# OnePercent MVP

Веб-приложение для системного управления жизнью и достижения целей через улучшение на 1% каждый день.

---

## Оглавление

- [Основные модули](#-основные-модули)
  - [ССП (Сбалансированная система показателей)](#1-ссп-сбалансированная-система-показателей)
  - [Банк Целей](#2-банк-целей)
  - [Декомпозиция](#3-декомпозиция)
  - [Планировщик](#4-планировщик)
  - [Онбординг](#5-онбординг)
  - [Мини-задачи](#6-мини-задачи)
  - [AI Ментор](#7-ai-ментор)
  - [First Steps](#8-first-steps)
  - [Дневник](#9-дневник)
  - [Центр обучения](#10-центр-обучения)
- [Технологии](#-технологии)
- [Установка и запуск](#-установка-и-запуск)
- [Конфигурация для разработки](#️-конфигурация-для-разработки)
- [Структура проекта](#️-структура-проекта)
- [Хранение данных](#-хранение-данных)
- [Интеграция с Django Backend](#-интеграция-с-django-backend)
  - [Общая архитектура](#общая-архитектура)
  - [API эндпоинты](#api-эндпоинты)
  - [ССП Backend Integration](#ссп-backend-integration)
  - [Mini-Task Backend Integration](#mini-task-backend-integration)
- [Аутентификация](#-аутентификация)
  - [Авторизация через Telegram](#авторизация-через-telegram)
  - [Модальные окна Telegram](#модальные-окна-telegram)
  - [Telegram бот в сайдбаре](#telegram-бот-в-сайдбаре)
- [Навигация по GET-параметрам](#-навигация-по-get-параметрам)
- [Дизайн-система](#-дизайн-система)
- [Ключевые принципы](#-ключевые-принципы)
- [Лицензия](#-лицензия)

---

## 🎯 Основные модули

### 1. ССП (Сбалансированная система показателей)

4-шаговый guided flow для оценки баланса жизни:

| Шаг | Название | Описание |
|-----|----------|----------|
| 0 | Приветствие | Вводный экран с описанием модуля |
| 1 | Теория | Объяснение методологии ССП |
| 2 | Колесо жизни | Оценка 6 сфер по шкале 1-10 |
| 3 | Рефлексия | Заполнение ответов на вопросы по каждой сфере |
| 4 | Итог | Сводка результатов, выявление зон роста |

**6 сфер жизни:**

| Frontend ID | Backend Code | Название | Иконка |
|-------------|--------------|----------|--------|
| `wealth` | `welfare` | Благосостояние | Wallet |
| `hobbies` | `hobby` | Хобби и отдых | Palette |
| `friendship` | `environment` | Дружба и окружение | Users |
| `health` | `health_sport` | Здоровье и спорт | Heart |
| `career` | `work` | Работа и карьера | Briefcase |
| `love` | `family` | Любовь, семья | HeartHandshake |

**Компоненты UI:**
- Интерактивное "Колесо жизни" с изогнутыми текстовыми метками (SVG textPath)
- Цветокодированные аккордеоны рефлексий с Lucide иконками
- Inline-редактирование рефлексий на странице итогов
- Статистические блоки: средний балл, сильная сторона, зона роста

### 2. Банк Целей

3-шаговый воркфлоу для работы с целями:

| Шаг | Описание |
|-----|----------|
| 1 | Валидация целей (проверка формулировок) |
| 2 | Фильтрация (выбор актуальных целей) |
| 3 | Трансфер (перенос в работу) |

**Финальный экран (Summary):**

| Функция | Описание |
|---------|----------|
| Отображение ВСЕХ целей | Показываются все добавленные цели, не только валидированные |
| Пагинация | 6 целей на страницу, кнопка "Загрузить ещё", фиксированная высота |
| Текстовый поиск | Поиск по названию и содержанию цели |
| Фильтры по статусу | Истинные, Отклонённые, Не оценённые, В работе, Завершённые, Доступные |
| Фильтр по сфере | Выбор сферы жизни |

**Расширенное модальное окно редактирования:**

| Поле | Описание |
|------|----------|
| Название цели | Текст цели |
| Сфера жизни | Выбор из 6 сфер |
| Почему эта цель мне важна? | Первый уровень "3 Почему" |
| Почему именно это даст мне то, что я хочу? | Второй уровень "3 Почему" |
| Почему это действительно про меня? | Третий уровень "3 Почему" |
| Кнопки оценки | "Это истинная цель" / "Это ложная цель" |
| Убрать из работы | Для целей в работе (вместо "Удалить") |

**Логика кнопок действий в таблице:**

Кнопки действий появляются динамически в зависимости от статуса цели и её состояния в работе.

| Кнопка | Иконка | Условие появления | Действие |
|--------|--------|-------------------|----------|
| Редактировать | Edit2 | **Всегда** для любой цели | Открывает модальное окно редактирования |
| Декомпозировать | GitBranch | `status === 'validated'` | Переход на страницу декомпозиции `/app/goals/{id}` |
| Взять в работу | Plus | `status === 'validated'` **И** цель **НЕ** в работе | Переносит цель в раздел "Мои цели" |
| Завершить | Check | Цель **в работе** **И** **НЕ** завершена | Отмечает цель как достигнутую |
| Убрать из работы | X | Цель **в работе** **И** **НЕ** завершена | Удаляет цель из работы (возвращает в банк) |
| Вернуть в работу | RotateCcw | Цель **завершена** | Возвращает завершённую цель обратно в работу |

**Определение состояний:**
- **В работе** (`isGoalTransferred`): цель существует в `store.goals` с `source === 'goals-bank'` и соответствующим `sourceId`
- **Завершена** (`isGoalCompleted`): цель в работе имеет `status === 'completed'`
- **Валидирована**: `rawIdea.status === 'validated'`

**Визуальные состояния кнопок:**
| Тип | Цвет при наведении | Назначение |
|-----|-------------------|------------|
| btn-icon-action | Фиолетовый (#6366f1) | Редактирование |
| btn-icon-decompose | Фиолетовый (#8b5cf6) | Декомпозиция |
| btn-icon-success | Зелёный (#10b981) | Положительные действия |
| btn-icon-danger | Красный (#ef4444) | Удаление/убрать |
| btn-icon-secondary | Серый (#6b7280) | Нейтральные действия |

**Статусы целей:**

| Статус | Цвет | Описание |
|--------|------|----------|
| Истинная | Зелёный (#22c55e) | Прошла проверку "3 Почему" |
| Отклонена | Красный (#ef4444) | Не прошла проверку |
| Не оценена | Оранжевый (#f59e0b) | Ещё не проверена |
| В работе | Фиолетовый (primary) | Взята в работу |
| Завершена | Изумрудный (#10b981) | Цель достигнута |

### 3. Декомпозиция (Страница деталей цели)

Разбиение целей на конкретные шаги (доступ через кнопку "Декомпозировать" в Банке целей):

- Создание целей с привязкой к сферам
- Добавление шагов с drag & drop сортировкой
- Чекбоксы выполнения шагов
- Комментарии к каждому шагу с авто-расширением (макс. 7 строк, далее скролл)
- Визуальное выделение завершённых шагов (зелёная граница, зачёркивание)
- Однокодочный layout (AI Coach удалён)

**Примечание:** Раздел "Декомпозиция" удалён из бокового меню. Доступ к декомпозиции осуществляется через кнопку в таблице Банка целей.

**Автосохранение и уведомления (ноябрь 2025):**
- Автоматическое сохранение после каждого действия (изменение шага, приоритета, времени, даты, статуса)
- Hash-based отслеживание изменений для предотвращения дублирующих toast-уведомлений
- Toast-уведомления об успешном сохранении (появляются в правом верхнем углу, исчезают через 3 сек)

**Параметры шага:**

| Параметр | Описание |
|----------|----------|
| Приоритет | Критично / Важно / Внимание / Опционально (цветовая индикация) |
| Время | 15 мин / 30 мин / 1 час / 2 часа / 3 часа / 4 часа |
| Дата | Выбор даты через календарь |
| Статус | Ожидает / В работе / Выполнен |
| Комментарий | Многострочное поле с авто-расширением (до 7 строк, далее скролл) |

**Многострочные комментарии:**
- При вводе: поле автоматически расширяется с каждой новой строкой (до 7 строк)
- При загрузке страницы: сохранённые многострочные комментарии отображаются с корректной высотой
- После 7 строк: появляется вертикальный скролл внутри поля ввода
- Функция `adjustAllCommentHeights()` вызывается при загрузке данных и при пагинации

**Подсветка приоритетов (ноябрь 2025):**

Шаги с приоритетом подсвечиваются цветом (аналогично Планировщику):

| Приоритет | Цвет левой границы | Фон |
|-----------|-------------------|-----|
| Критично | Красный (#ef4444) | Светло-красный |
| Важно | Оранжевый (#f97316) | Светло-оранжевый |
| Внимание | Синий (#3b82f6) | Светло-синий |
| Опционально | Серый (#9ca3af) | Светло-серый |

**Фильтры шагов:**

| Фильтр | Описание |
|--------|----------|
| Текстовый поиск | Поиск по названию и комментарию шага |
| Статус | Выполнены / Не выполнены |
| Приоритет | Критично / Важно / Внимание / Опционально |
| Сброс | Кнопка для очистки всех фильтров |

**Сортировка шагов:**

Дропдаун в правой части строки фильтров позволяет сортировать шаги:

| Вариант | Описание |
|---------|----------|
| По порядку | Исходный порядок (по умолчанию) |
| По приоритету | Критично → Важно → Внимание → Опционально |
| По статусу | В работе → Ожидает → Выполнен |
| По времени | 15 мин → 30 мин → 1 час → 2 часа → 3 часа → 4 часа |
| По дате | По запланированной дате |

Кнопка со стрелками переключает направление сортировки (по возрастанию / по убыванию).

**Примечание:** Drag & drop отключён при сортировке не "По порядку".

**Пагинация шагов:**
- При загрузке отображается 10 шагов
- Кнопка "Загрузить ещё N шагов"
- Вертикальный скролл при превышении лимита

**Добавление нового шага:**
- При клике "Добавить шаг" новый шаг появляется в конце списка и сразу виден (пагинация расширяется)
- Пустые шаги (без названия) не сохраняются — сохранение происходит только после ввода названия
- Хэш-функция исключает пустые шаги для корректного отслеживания изменений

**Оптимизированный заголовок (ноябрь 2025):**

| Элемент | Позиция | Описание |
|---------|---------|----------|
| Название цели | Слева | Обрезается на "..." если не помещается |
| Статус | Справа вверху | Бейдж со статусом цели |
| Категория | Справа под статусом | Иконка и название сферы жизни |
| Редактировать | Под названием, по левому краю | Кнопка редактирования цели |

**Кнопка удаления шага:**
- Стилизована как в Банке целей (btn-icon btn-icon-danger)
- При наведении становится красной

**Модальное окно редактирования:**

Унифицировано с Банком целей:
- Разделитель "Правило 3 Почему"
- Выбор сферы жизни (сетка иконок)
- Три текстовых поля для "Почему?"
- Кнопки оценки: "Это истинная цель" / "Это ложная цель"
- Footer: "Удалить" слева, "Отмена" и "Сохранить" справа

**Навигация:**
- Кнопка "Планировщик" → быстрый переход к планированию шагов
- Кнопка "Сохранить" → переход в Банк целей с сохранением фильтров
- Кнопка "Редактировать цель" → открывает модальное окно (как в Банке целей) с полями "3 Почему"
- Кнопка "Назад" → возврат в Банк целей с восстановлением фильтров

### 4. Планировщик

Ежедневное и недельное планирование:

| Функция | Описание |
|---------|----------|
| 5-дневная рабочая неделя | Сворачиваемые выходные |
| Навигация по неделям | Вперёд/назад по календарю |
| Цветовые приоритеты | Красный → Оранжевый → Синий → Серый |
| Drag & Drop | Перетаскивание задач между днями |
| Быстрое добавление | Quick-add с автофокусом |
| Undo удаления | Отмена удаления задачи |
| Дневной бюджет времени | Индикатор загруженности |
| Недельная статистика | Обзор выполнения |

**Система фильтрации (ноябрь 2025):**

| Фильтр | Описание |
|--------|----------|
| Текстовый поиск | Поиск по названию цели и шагов |
| Фильтр по сфере | Dropdown со всеми сферами жизни |
| Фильтр по статусу | Запланированы / Не запланированы / Частично / Полностью |
| Эта неделя | Показывает только цели с шагами на текущей неделе |
| Сброс фильтров | Кнопка для очистки всех фильтров |

**Пагинация:**

| Элемент | Лимит | Поведение |
|---------|-------|-----------|
| Цели | 10 | Кнопка "Загрузить ещё N целей" |
| Шаги внутри цели | 6 | Кнопка "Ещё N шагов" для каждой цели |
| Вертикальный скролл | Auto | Появляется при превышении лимита |

**Отображение даты в шагах:**

Для каждого запланированного шага отображается:
- День недели (Пн, Вт, Ср...)
- Дата в формате "15 дек"

**Пустое состояние:**

Если нет целей с шагами — кнопка "Перейти к декомпозиции" ведёт на `/app/goals-bank`

### 5. Онбординг

3-шаговый процесс знакомства:

| Шаг | Содержание |
|-----|------------|
| 1 | Философия (принцип 1%) |
| 2 | Точки A и B (откуда → куда) |
| 3 | Правила работы |

- Опциональные поля с возможностью пропуска
- Lucide иконки вместо эмодзи
- Сохранение данных на backend при завершении

### 6. Мини-задачи

4-шаговый модуль для быстрого старта:

| Шаг | Название | Описание |
|-----|----------|----------|
| 1 | Brain Dump | Выгрузка всех задач из головы |
| 2 | Категоризация | Распределение по 4 категориям |
| 3 | Выбор действий | Отметка приоритетных задач |
| 4 | Выполнение | Чекбоксы завершения |

**4 категории (hardcoded на фронте):**

| Frontend ID | Backend Code | Название | Иконка |
|-------------|--------------|----------|--------|
| `calendar` | `calendar` | Календарь | 🗓️ |
| `next` | `action` | Следующие действия | ✅ |
| `someday` | `idea` | Идеи / Когда-нибудь | 💡 |
| `reference` | `info` | Справка | 📚 |

### 7. AI Ментор

Центральная ценность продукта — персонализированный коуч, помогающий пользователю во всех модулях.

| Компонент | Расположение | Функции |
|-----------|--------------|---------|
| MentorWidget | Dashboard | Полноценный чат с приветствием, быстрыми подсказками |
| MentorFloatingButton | Все страницы (кроме Dashboard) | Плавающая кнопка с контекстными хинтами |

**Режимы работы:**
- Проактивный — ментор сам предлагает помощь
- По запросу — только когда пользователь обращается
- Выключен — без подсказок

**Интеграция с First Steps:**
При выполнении каждого шага ментор добавляет персональное сообщение поддержки.

### 8. First Steps

7-шаговый чеклист для быстрого старта новых пользователей:

| Шаг | Действие | Триггер | Следующий модуль |
|-----|----------|---------|------------------|
| 1 | Оценить баланс жизни | Завершение ССП | Банк целей |
| 2 | Добавить идею | Добавление в Банк целей | — |
| 3 | Валидировать цель | Проверка цели | — |
| 4 | Выбрать ключевую цель | Перенос целей | Планирование |
| 5 | Запланировать задачу | Добавление в план | Дневник |
| 6 | Написать в дневник | Сохранение записи | AI Ментор |
| 7 | Пообщаться с ментором | Отправка сообщения | — |

**Toast-уведомления:**
- Появляются в правом верхнем углу при выполнении шага
- Содержат CTA-кнопку для перехода к следующему шагу
- Автоматически исчезают через 5 секунд

### 9. Дневник

Ежедневная рефлексия с 4 вопросами:

| Вопрос | Назначение |
|--------|------------|
| Что удалось сделать? | Фиксация достижений |
| Что не удалось? | Анализ препятствий |
| Что понял/осознал? | Извлечение уроков |
| Планы на завтра | Подготовка к следующему дню |

**Функции:**
- Трекинг серии (streak)
- Календарь с историей записей
- AI-ответы ментора (демо-режим)
- Модальная форма записи

### 10. Центр обучения

Страница `/app/learning` с обучающими материалами:

| Урок | Содержание |
|------|------------|
| Философия 1% | Принцип маленьких улучшений |
| ССП | Методология баланса жизни |
| Целеполагание | SMART-цели, MVP подход |
| Декомпозиция | Разбиение на шаги |
| Планирование | Недельный ритм |
| Дневник | Польза рефлексии |
| AI Ментор | Как работать с коучем |

Уроки можно отмечать как пройденные с отслеживанием прогресса.

---

## 🚀 Технологии

| Технология | Назначение |
|------------|------------|
| Vue 3 | Основной фреймворк (Composition API, script setup) |
| Pinia | Управление состоянием с localStorage persistence |
| Vue Router | Маршрутизация с auth guards |
| Vite | Сборщик с proxy для Django backend |
| Lucide Vue Next | Минималистичные иконки |

---

## 📦 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production-сборки
npm run preview
```

---

## ⚙️ Конфигурация для разработки

Настройки: `src/config/local_settings.js`

```javascript
export const DEV_MODE = true
export const SKIP_AUTH_CHECK = false
export const API_BASE_URL = 'https://your-server.com'
export const DEBUG_MODE = true
export const FORCE_SHOW_ONBOARDING = false
export const FORCE_SHOW_MINITASK = false
```

| Параметр | Описание |
|----------|----------|
| `DEV_MODE` | Режим разработки |
| `SKIP_AUTH_CHECK` | Пропуск проверки авторизации |
| `API_BASE_URL` | URL бэкенда ('' = proxy) |
| `DEBUG_MODE` | Логирование API в консоль |
| `FORCE_SHOW_ONBOARDING` | Принудительный показ онбординга |
| `FORCE_SHOW_MINITASK` | Принудительный показ мини-задач |

### Подключение к Django API

На сервере Django настройте CORS:

```python
CORS_ALLOW_ALL_ORIGINS = True
CORS_ALLOW_CREDENTIALS = True
```

---

## 🏗️ Структура проекта

```
src/
├── assets/          # Стили и ресурсы
├── components/      # Переиспользуемые компоненты
│   ├── WheelOfLife.vue
│   ├── TelegramAuthModals.vue
│   ├── MentorWidget.vue           # Виджет AI Ментора (Dashboard)
│   ├── MentorFloatingButton.vue   # Плавающая кнопка ментора
│   ├── ToastNotification.vue      # Toast-уведомления
│   ├── FirstStepsWidget.vue       # Виджет 7 шагов
│   └── ...
├── config/          # Конфигурация
│   ├── settings.js
│   └── local_settings.js
├── services/        # API клиент
│   └── api.js
├── stores/          # Pinia stores
│   ├── app.js       # Основной store
│   └── toast.js     # Store для уведомлений
├── views/           # Страницы
│   ├── BalancedScorecard.vue (ССП)
│   ├── GoalsBank.vue
│   ├── Goals.vue (Декомпозиция)
│   ├── Planning.vue
│   ├── Onboarding.vue
│   ├── MiniTask.vue
│   ├── Journal.vue (Дневник)
│   └── Learning.vue (Центр обучения)
├── router/          # Маршрутизация
│   └── index.js
├── main.js          # Точка входа
└── App.vue          # Главный компонент
```

---

## 💾 Хранение данных

**LocalStorage ключи:**

| Ключ | Данные |
|------|--------|
| `onepercent_ssp` | Данные ССП |
| `onepercent_goals` | Цели и шаги |
| `onepercent_tasks` | Задачи |
| `onepercent_weeks` | Недельные планы |
| `onepercent_first_steps` | Прогресс First Steps (7 шагов) |
| `onepercent_mentor` | Настройки и история AI Ментора |
| `onepercent_journal` | Записи дневника |
| `onepercent_learning` | Прогресс обучения |
| `day_focus_*` | Фокус дня |
| `day_reflection_*` | Рефлексия дня |

---

## 🔌 Интеграция с Django Backend

### Общая архитектура

**Layered Backend Sync** — паттерн, при котором:
1. Локальное состояние сохраняется для быстрого UX
2. Изменения автоматически синхронизируются с бэкендом
3. При загрузке страницы данные подтягиваются с сервера

### API эндпоинты

| Модуль | GET | POST/UPDATE |
|--------|-----|-------------|
| Аутентификация | `/api/rest/front/get-user-data/` | - |
| Глобальные данные | `/api/rest/front/get-global-data/` | - |
| CSRF | `/api/rest/front/csrf/` | - |
| ССП | `/api/rest/front/app/ssp/get/` | `/api/rest/front/app/ssp/update/` |
| Мини-задачи | `/api/rest/front/app/onboard/mini-task/get/` | `/api/rest/front/app/onboard/mini-task/update/` |
| Онбординг | - | `/api/rest/front/app/onboard/update/` |
| **Цели (Goals)** | `/api/rest/front/app/goals/get/` | `/api/rest/front/app/goals/update/` |
| **Шаги целей** | `/api/rest/front/app/goals/steps/get/` | `/api/rest/front/app/goals/steps/update/` |

### ССП Backend Integration

**Файлы:** `api.js`, `app.js` (store), `BalancedScorecard.vue`

**Структура данных бэкенда:**

```javascript
{
  max_rating: 10,
  base_categories_info: [...],     // Справочник категорий
  circle_data: [...],              // Оценки (шаг 2)
  categories_reflection_data: [...], // Рефлексии (шаг 3)
  total_data: {...}                // Статистика (шаг 4)
}
```

**Store методы (app.js):**

| Метод | Описание |
|-------|----------|
| `loadSSPFromBackend()` | Загрузка данных при монтировании |
| `saveSSPToBackend()` | Базовый метод сохранения |
| `saveSSPRatingsToBackend()` | Сохранение оценок (шаг 2) |
| `saveSSPReflectionToBackend(sphereId)` | Сохранение рефлексии одной сферы |
| `saveAllSSPReflectionsToBackend()` | Сохранение всех рефлексий |
| `syncLifeSpheresFromBackend(data)` | Синхронизация локального состояния |

**Маппинг категорий:**

```javascript
// frontend → backend
const SPHERE_TO_BACKEND = {
  wealth: 'welfare',
  hobbies: 'hobby',
  friendship: 'environment',
  health: 'health_sport',
  career: 'work',
  love: 'family'
}

// backend → frontend
const BACKEND_TO_SPHERE = {
  welfare: 'wealth',
  hobby: 'hobbies',
  environment: 'friendship',
  health_sport: 'health',
  work: 'career',
  family: 'love'
}
```

**Триггеры автосохранения:**

| Событие | Действие |
|---------|----------|
| Переход шаг 2 → 3 | `saveSSPRatingsToBackend()` |
| Переход шаг 3 → 4 | `saveAllSSPReflectionsToBackend()` |
| Сохранение рефлексии | `saveSSPReflectionToBackend(sphereId)` |
| Завершение модуля | `saveAllSSPReflectionsToBackend()` |

**Resume Flow (логика навигации при открытии `/app/ssp`):**

```
loadSSPFromBackend() → проверка totalData
                ↓
┌───────────────────────────────────────────────────────────┐
│ Проверка: categories_with_rating=0 ИЛИ user_rating=0     │
│           ИЛИ reflection_questions_answers=0              │
└───────────────────────────────────────────────────────────┘
                ↓                          ↓
          [НЕТ ДАННЫХ]              [ЕСТЬ ДАННЫЕ]
                ↓                          ↓
          step 0 (приветствие)    проверка sspModuleCompleted
                                           ↓
                              ┌────────────┴────────────┐
                        [ЗАВЕРШЁН]                [НЕ ЗАВЕРШЁН]
                              ↓                          ↓
                        step 4 (итог)           определение шага:
                                                - reflection_answers > 0 → step 3
                                                - categories_with_rating > 0 → step 2
                                                - иначе → step 1
```

**Отображение рефлексии на шаге 4:**
- Формат: `{reflection_questions_answers}/{reflection_questions_total}` (например: `6/24`)
- Данные берутся из `totalData` ответа бэкенда

**Кнопка "Пройти заново":**
- Сбрасывает флаг `sspModuleCompleted.completed` в localStorage
- Возвращает на шаг 1 (теория)

**GET-параметр `spp_step`** может перезаписать автоопределение шага

### Mini-Task Backend Integration

**Store методы (app.js):**

| Метод | Описание |
|-------|----------|
| `loadMiniTaskFromBackend()` | Загрузка данных |
| `saveMiniTaskToBackend()` | Полное сохранение |
| `saveMiniTaskTasks()` | Сохранение задач |
| `updateMiniTaskStep()` | Обновление шага |
| `completeMiniTaskWithBackend()` | Завершение модуля |
| `resetMiniTask()` | Сброс данных |

**Маппинг категорий:**

```javascript
// frontend → backend
{ calendar: 'calendar', next: 'action', someday: 'idea', reference: 'info' }

// backend → frontend
{ calendar: 'calendar', action: 'next', idea: 'someday', info: 'reference' }
```

### Goals API Integration (Банк целей, Планирование, Детали цели)

**Файлы:** `src/services/api.js`, `src/stores/app.js`

**API эндпоинты:**

| Эндпоинт | Описание | Используется на |
|----------|----------|-----------------|
| `/api/rest/front/app/goals/get/` | Получение списка целей | Банк целей, Планирование |
| `/api/rest/front/app/goals/steps/get/` | Получение шагов цели | Детали цели, Планирование |
| `/api/rest/front/app/goals/update/` | Создание/обновление целей | Все страницы с целями |
| `/api/rest/front/app/goals/steps/update/` | Создание/обновление шагов | Детали цели, Планирование |

#### Справочные данные (Global Data)

Эндпоинт `/api/rest/front/get-global-data/` теперь возвращает справочники для целей и шагов:

| Поле | Описание | Пример значений |
|------|----------|-----------------|
| `goals_categories_data` | Категории целей | welfare, hobby, environment, health_sport, work, family |
| `goals_types_data` | Типы оценки целей | true (Истинная), false (Ложная) |
| `goals_statuses_data` | Статусы целей | work (В работе), complete (Завершенные) |
| `goals_statuses_filter_data` | Статусы для фильтров | + unstatus (Не оцененные) |
| `step_statuses_filter_data` | Статусы шагов | planned, unplanned |
| `step_results_filter_data` | Результаты шагов | complete, uncomplete |
| `times_data` | Варианты времени | half (30м), one (1ч), two (2ч), three (3ч), four (4ч) |
| `priority_data` | Приоритеты | critical, important, attention, optional |

#### GET Goals (`/api/rest/front/app/goals/get/`)

**Параметры запроса:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `with_steps_data` | boolean | Получить данные по шагам (первая страница) |
| `order_by` | string | Поле сортировки: date_created, status, title, category |
| `order_direction` | string | Направление: asc, desc |
| `status_filter` | string | Фильтр: work, complete, unstatus |
| `category_filter` | string | Фильтр по категории |
| `query_filter` | string | Текстовый поиск (min 3 символа) |
| `page` | number | Номер страницы |

**Структура ответа:**

```javascript
{
  status: "ok",
  data: {
    goals_data: [
      {
        goal_id: 1,
        title: "Название цели",
        category: "health_sport",
        status: "work",           // work | complete | null
        score: "true",            // true | false | null (оценка истинности)
        why_important: "...",
        why_give_me: "...",
        why_about_me: "...",
        date_created: "2025-12-01 09:39:12",
        date_completed: null,
        total_steps_data: { total_steps: 3, complete_steps: 1, complete_percent: 33 },
        steps_data: [...]         // если with_steps_data=true
      }
    ],
    total_data: {
      total_goals: 40,
      true_goals: 15,
      false_goals: 8,
      in_work_goals: 18
    },
    total_data_steps_data: {...}, // если with_steps_data=true
    page: 1,
    page_size: 6,
    total_pages: 7,
    total_items: 40,
    total_filtered_items: 40
  }
}
```

#### GET Goal Steps (`/api/rest/front/app/goals/steps/get/`)

**Параметры запроса:**

| Параметр | Тип | Описание |
|----------|-----|----------|
| `goal_id` | number | ID цели (обязательный) |
| `order_by` | string | Поле сортировки: order, date_created, status, priority |
| `order_direction` | string | Направление: asc, desc |
| `status_filter` | string | planned, unplanned |
| `result_filter` | string | complete, uncomplete |
| `priority_filter` | string | critical, important, attention, optional |
| `query_filter` | string | Текстовый поиск |
| `page` | number | Номер страницы |
| `page_size` | number | Размер страницы (6-10) |

**Структура шага:**

```javascript
{
  step_id: 1,
  goal_id: 1,
  title: "Название шага",
  description: "Описание/комментарий",
  status: "planned",        // planned | unplanned
  priority: "important",    // critical | important | attention | optional
  time_duration: "one",     // half | one | two | three | four
  dt: "2025-01-14",        // Запланированная дата (YYYY-MM-DD)
  order: 1,
  is_complete: false,
  date_created: "2025-12-01 16:12:22",
  date_completed: null
}
```

#### UPDATE Goals (`/api/rest/front/app/goals/update/`)

**Параметры запроса:**

```javascript
{
  goals_data: [
    {
      goal_id: null,          // null для создания, число для обновления
      title: "Название",
      category: "hobby",
      status: "work",
      score: "true",
      why_important: "...",
      why_give_me: "...",
      why_about_me: "..."
    }
  ],
  deleted_goals_ids: [1, 2],   // ID целей для удаления
  completed_goals_ids: [3, 4]  // ID целей для завершения
}
```

#### UPDATE Steps (`/api/rest/front/app/goals/steps/update/`)

**Параметры запроса:**

```javascript
{
  goals_steps_data: [
    {
      goal_id: 1,             // обязательный
      step_id: null,          // null для создания, число для обновления
      title: "Название",
      description: "Описание",
      priority: "important",
      time_duration: "one",
      dt: "2025-01-14",
      order: 1,
      is_complete: false,
      is_deleted: false       // true для удаления
    }
  ]
}
```

#### Helper-функции в API сервисе

| Функция | Описание |
|---------|----------|
| `getGoals(params)` | Получить список целей |
| `getGoalSteps(params)` | Получить шаги цели |
| `updateGoals(data)` | Обновить цели (batch) |
| `updateGoalSteps(data)` | Обновить шаги (batch) |
| `createGoal(goalData)` | Создать одну цель |
| `updateGoal(goalId, goalData)` | Обновить одну цель |
| `deleteGoal(goalId)` | Удалить цель |
| `completeGoal(goalId)` | Завершить цель |
| `createStep(goalId, stepData)` | Создать шаг |
| `updateStep(goalId, stepId, stepData)` | Обновить шаг |
| `deleteStep(goalId, stepId)` | Удалить шаг |
| `toggleStepComplete(goalId, stepId, isComplete)` | Отметить завершение |
| `scheduleStep(goalId, stepId, date, priority, timeDuration)` | Запланировать шаг |

#### Store: Global Data

**Методы:**

| Метод | Описание |
|-------|----------|
| `loadGlobalData()` | Загрузка справочников при старте приложения |
| `getCategoryTitle(id)` | Получить название категории |
| `getPriorityTitle(id)` | Получить название приоритета |
| `getTimeTitle(id)` | Получить название времени |

**Маппинг категорий:**

```javascript
// Backend ID → Frontend ID (сферы жизни)
const categoryBackendToFrontend = {
  welfare: 'wealth',
  hobby: 'hobbies',
  environment: 'friendship',
  health_sport: 'health',
  work: 'career',
  family: 'love'
}

// Frontend ID → Backend ID
const categoryFrontendToBackend = {
  wealth: 'welfare',
  hobbies: 'hobby',
  friendship: 'environment',
  health: 'health_sport',
  career: 'work',
  love: 'family'
}
```

---

## 🔐 Аутентификация

Cookie-based аутентификация с CSRF-защитой.

### Авторизация через Telegram

**Схема работы:**

```
1. Клик "Войти через Telegram"
   ↓
2. GET /api/rest/front/get-global-data/ → получение t_auth_link
   ↓
3. Редирект на oauth.telegram.org
   ↓
4. Возврат с #tgAuthResult=... в URL
   ↓
5. Декодирование base64 данных
   ↓
6. Редирект на t_auth_callback_url + query params
   ↓
7. Django авторизует → редирект на /app/
```

### Модальные окна Telegram

Компонент `TelegramAuthModals.vue` обрабатывает GET-параметры:

| Параметр | Описание |
|----------|----------|
| `?telegram_auth_error=token_expired` | Ссылка истекла (10 мин) |
| `?telegram_auth_error=token_not_found` | Ссылка недействительна |
| `?telegram_complete_registration=1` | Форма email/пароль для нового пользователя |

### Telegram бот в сайдбаре

- Кнопка "Телеграм бот" под именем пользователя
- Показывается только если `telegram_bot_link` получен с бэкенда
- Модальное окно с персональной ссылкой и кнопкой копирования

---

## 🔗 Навигация по GET-параметрам

### ССП модуль

URL: `/app/ssp?spp_step=N`

| Значение | Экран |
|----------|-------|
| `spp_step=0` | Приветствие |
| `spp_step=1` | Теория |
| `spp_step=2` | Колесо жизни |
| `spp_step=3` | Рефлексия |
| `spp_step=4` | Итог |

### Планировщик

URL: `/app/planning?search=...&sphere=...&status=...&week=1`

| Параметр | Значения | Описание |
|----------|----------|----------|
| `search` | Текст | Поиск по названию цели и шагов |
| `sphere` | `wealth`, `hobbies`, `friendship`, `health`, `career`, `love` | Фильтр по сфере жизни |
| `status` | `scheduled`, `unscheduled`, `partial`, `full` | Статус планирования |
| `week` | `1` | Показать только цели с шагами на текущей неделе |

При изменении фильтров URL обновляется автоматически. При переходе на страницу фильтры восстанавливаются из URL.

### Банк целей

URL: `/app/goals-bank?search=...&sphere=...&status=...&edit=...`

| Параметр | Значения | Описание |
|----------|----------|----------|
| `search` | Текст | Поиск по названию цели |
| `sphere` | `wealth`, `hobbies`, `friendship`, `health`, `career`, `love` | Фильтр по сфере жизни |
| `status` | `validated`, `rejected`, `pending`, `in_work`, `completed`, `available` | Статус цели |
| `edit` | ID цели | Автоматически открывает модал редактирования цели |

При изменении фильтров URL обновляется автоматически. Параметр `edit` сохраняется при смене фильтров.

---

## 🎨 Дизайн-система

| Элемент | Реализация |
|---------|------------|
| Иконки | Lucide Vue Next (минималистичные линейные) |
| Цвета приоритетов | Красный → Оранжевый → Синий → Серый |
| Темы | Светлая / Тёмная |
| Layout | Коллапсируемый сайдбар |
| Анимации | Плавные переходы |

---

## 🌟 Ключевые принципы

1. **Эффект 1%** — маленькие ежедневные улучшения
2. **Системность** — структура вместо мотивации
3. **Баланс** — гармоничное развитие всех сфер жизни
4. **Осознанность** — рефлексия и понимание "почему"
5. **Декомпозиция** — разбиение больших целей на шаги

---

## 📝 История изменений

### Декабрь 2025

**Исправление инициализации модуля ССП:**
- Исправлена логика определения начального шага на основе данных API
- Три сценария обработки:
  1. Нет данных (categories=0, user_rating=0, reflections=0) → приветственный экран (step 1, lessonStarted=false)
  2. Прошёл ССП: `(user_rating > 0 OR reflection_questions_answers > 0) AND categories_with_rating == 6` → финальный экран (step 4)
  3. Частичные данные → начинается со step 2 (после теории)

**Удаление статуса шага из страницы редактирования цели (GoalEdit):**
- Убран dropdown со статусами (Ожидает / В работе / Выполнен) из карточек шагов
- Теперь завершение шага управляется только через чекбокс
- Удалена функция `updateStepStatus` и связанные CSS стили

**Модальное окно добавления новой цели (Банк целей):**
- Кнопка "Добавить новую цель" на финальной странице Банка целей теперь открывает модальное окно вместо перехода на начальные экраны
- Модальное окно содержит:
  - Название цели
  - Выбор сферы жизни (сетка иконок)
  - Правило "3 Почему" (три текстовых поля)
  - Кнопки оценки: "Это истинная цель" / "Это ложная цель"
- Footer модального окна: "Отмена" слева, "Добавить цель" справа (кнопки разнесены по краям)
- Убраны элементы, присутствующие в модале редактирования:
  - Кнопка "Убрать из работы" / "Удалить"
  - Секция "Нужна декомпозиция на шаги?"
- При добавлении новая цель появляется первой в таблице (вверху списка)
- Кнопка "Добавить цель" неактивна (disabled) пока не введено название

---

## 📄 Лицензия

MIT License — свободное использование
