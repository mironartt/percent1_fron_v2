# OnePercent MVP

Веб-приложение для системного управления жизнью и достижения целей через улучшение на 1% каждый день.

## 🎯 Основные модули

### 1. ССП (Сбалансированная система показателей)
- Оценка 6 ключевых сфер жизни (Здоровье, Карьера, Финансы, Отношения, Личностный рост, Досуг)
- Визуализация баланса между сферами
- Определение зон роста и сильных сторон
- Заметки и рефлексия по каждой сфере

### 2. Цели и декомпозиция
- Создание целей с привязкой к сферам жизни
- Формулирование MVP (минимально жизнеспособного результата)
- Осознание "почему важно" для внутренней мотивации
- Декомпозиция целей на конкретные шаги
- Отслеживание прогресса выполнения

### 3. Планировщик
- Ежедневное планирование с приоритетами
- Недельное планирование и цели
- Управление энергией (высокая/средняя/низкая)
- Утренний фокус и вечерняя рефлексия
- Визуализация прогресса

## 🚀 Технологии

- **Vue 3** - основной фреймворк
- **Pinia** - управление состоянием
- **Vue Router** - маршрутизация
- **Vite** - сборщик и dev-сервер
- **LocalStorage** - хранение данных

## 📦 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production-сборки
npm run preview
```

## 🏗️ Структура проекта

```
src/
├── assets/          # Стили и ресурсы
├── components/      # Переиспользуемые компоненты
├── stores/          # Pinia stores (состояние)
├── views/           # Страницы приложения
├── router.js        # Маршрутизация
├── main.js          # Точка входа
└── App.vue          # Главный компонент
```

## 💾 Хранение данных

Все данные хранятся локально в браузере (LocalStorage):
- `onepercent_ssp` - данные ССП
- `onepercent_goals` - цели и их шаги
- `onepercent_tasks` - задачи
- `onepercent_weeks` - недельные планы
- `day_focus_*` - фокус дня
- `day_reflection_*` - рефлексия дня

## 🎨 Дизайн-система

Приложение использует современный, минималистичный дизайн с:
- Адаптивной версткой для мобильных устройств
- Плавными анимациями и переходами
- Интуитивными иконками и эмодзи
- Цветовым кодированием приоритетов
- Прогресс-барами для визуализации

## 🔄 Интеграция с Telegram

Данная веб-версия спроектирована для работы параллельно с Telegram-ботом:
- Web - для глубокой работы, планирования, аналитики
- Telegram - для быстрых напоминаний и ежедневных чек-инов

### Авторизация через Telegram (страница логина)

На странице `/auth/login` доступна кнопка "Войти через Telegram" (синяя, фирменный стиль Telegram).

**Схема работы:**
```
1. Клик по кнопке "Войти через Telegram"
   ↓
2. Запрос к /api/rest/front/get-global-data/ для получения t_auth_link
   ↓
3. Редирект на oauth.telegram.org (авторизация в Telegram)
   ↓
4. Возврат на страницу логина с #tgAuthResult=... в хеше
   ↓
5. JavaScript декодирует base64 данные из хеша
   ↓
6. Редирект на Django callback (t_auth_callback_url + query params)
   ↓
7. Django авторизует пользователя и редиректит на /app/
```

**API эндпоинт:** `POST /api/rest/front/get-global-data/`

**Ответ:**
```json
{
  "status": "ok",
  "data": {
    "t_auth_link": "https://oauth.telegram.org/auth?bot_id=...&origin=...&request_access=write&return_to=...",
    "t_auth_callback_url": "https://domain.com/telegram/auth/callback/"
  }
}
```

**Обработка возврата из Telegram:**
- При загрузке страницы проверяется наличие `#tgAuthResult=` в URL
- Данные декодируются из base64 → JSON
- Формируется URL: `t_auth_callback_url + ?id=...&hash=...&first_name=...`
- Выполняется редирект на Django callback

### Кнопка "Телеграм бот" в сайдбаре

В левом меню (под именем пользователя) отображается кнопка "Телеграм бот":
- Показывается только если поле `telegram_bot_link` получено с бэкенда
- Иконка Send в фирменном синем цвете Telegram (#0088cc)
- При свёрнутом сайдбаре показывается только иконка с tooltip

**По клику открывается модальное окно:**
- Заголовок с иконкой Telegram в синем градиенте
- Описание возможностей бота
- Персональная ссылка с кнопкой копирования (с анимацией подтверждения)
- Кнопка "Открыть в Telegram" для перехода в бота

### Модальные окна авторизации через Telegram

Компонент `TelegramAuthModals.vue` обрабатывает GET-параметры на всех страницах приложения:

| GET-параметр | Описание | UI |
|--------------|----------|-----|
| `?telegram_auth_error=token_expired` | Ссылка для входа истекла (срок действия 10 минут) | Оранжевая модалка с предупреждением |
| `?telegram_auth_error=token_not_found` | Ссылка недействительна или уже использована | Оранжевая модалка с предупреждением |
| `?telegram_complete_registration=1` | Успешная регистрация через Telegram | Зелёная модалка с формой email/пароль |

**Модалки ошибок (`token_expired`, `token_not_found`):**
- Оранжевая иконка предупреждения
- Описание проблемы и рекомендации
- Кнопки: "Войти через Telegram", "Войти", "Регистрация"

**Модалка завершения регистрации (`telegram_complete_registration=1`):**
- Зелёная иконка успеха
- Форма для указания email и пароля:
  - Email (обязательное поле)
  - Пароль (минимум 8 символов)
  - Подтверждение пароля
  - Переключатель видимости пароля
  - Валидация совпадения паролей в реальном времени
- Кнопка "Сохранить данные"
- Кнопка "Пропустить" для отложенного заполнения

**Поведение:**
- GET-параметры автоматически удаляются из URL при закрытии модалки
- Отслеживание изменений `route.query` для динамических переходов

## 🌟 Ключевые принципы

1. **Эффект 1%** - маленькие ежедневные улучшения
2. **Системность** - структура вместо мотивации
3. **Баланс** - гармоничное развитие всех сфер жизни
4. **Осознанность** - рефлексия и понимание "почему"
5. **Декомпозиция** - разбиение больших целей на шаги

## 📱 Будущие улучшения

- [ ] Синхронизация с Telegram-ботом через API
- [ ] Экспорт/импорт данных
- [ ] Темная тема
- [ ] Графики и визуализация прогресса
- [ ] Привычки и трекинг
- [ ] Шаблоны целей и планов
- [ ] Напоминания через браузер

## 📄 Лицензия

MIT License - свободное использование

## 👥 Контакты

Для вопросов и предложений: [ваш контакт]
